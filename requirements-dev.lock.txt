# Task E2-R1 dependency lock. Generated by resolving the declared set in a
# pristine virtual environment and recording the versions that were actually
# installed and verified there. Do not edit by hand.
# 
# resolved on: CPython 3.12.14, Windows AMD64
# platform tag: cpython-3.12-amd64
# resolver: pip 26.2.1
# build backend: setuptools provided by build isolation
# declared requires-python: >=3.12
# 
# PYTHON 3.12 IS THE SUPPORTED FLOOR AND THE ONLY TESTED VERSION. Task E2
# originally expected 3.11; that expectation was found incompatible with the
# resolved set (numpy requires >=3.12) and is preserved as history rather
# than restated as a claim. 3.13 and later are permitted by metadata and are
# NOT tested here.
# 
# PLATFORM-SCOPED, NOT UNIVERSAL. pandas, numpy, pyarrow and matplotlib ship
# platform-specific binary wheels, so this file pins versions rather than
# guaranteeing an identical byte-for-byte environment on another operating
# system. It says so instead of claiming a reproducibility it cannot deliver.
# 
# scope: development (project.optional-dependencies.dev and transitive)
# install alongside requirements.lock.txt, not instead of it
#
build==1.6.0
colorama==0.4.6
iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
pygments==2.21.0
pyproject-hooks==1.2.0
pytest==9.1.1
ruff==0.16.5

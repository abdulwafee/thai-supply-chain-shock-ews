"""Time-based splitting and evaluation metrics.

Unlike most other subpackages at this stage, `splits.py` and `metrics.py`
contain real, working implementations — they are generic evaluation
mechanics (date-ordered partitioning, standard regression/classification
metric formulas), not data-ingestion, feature-engineering, target
construction, or model-training business logic, so they are in scope for
this scaffolding step.
"""

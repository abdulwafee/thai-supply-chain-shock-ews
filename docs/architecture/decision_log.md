# Architecture Decision Log

Tracks open and resolved architecture decisions for the Thai Supply Chain Shock Early Warning
System's data layer. New entries are appended, never rewritten in place — a decision that changes
gets a new dated entry, and the old one is marked superseded rather than deleted, mirroring the
append-only policy used for the data itself (`data_architecture.md` §7).

## Open

| ID | Decision needed | Raised | Status |
|---|---|---|---|
| AD-01 | `processed/industry_month_panel`: wide vs. long as source count grows | 2026-08-26 | Open — wide chosen for MVP |
| AD-02 | Adopt DuckDB as a committed query layer, or keep exploratory-only | 2026-08-26 | Open |
| AD-03 | Where `oie_group_code` fits once/if OIE's own MPI grouping is verified | 2026-08-26 | Open |
| AD-04 | TSIC Division 12 (tobacco) — assign, create a 13th industry_id, or exclude | 2026-08-26 (carried from taxonomy design phase) | **Resolved 2026-08-27 by AD-R8** — assigned to IND-01 |
| AD-05 | `io_sector_code` real scheme, pending NESDC edition/sector-list verification | 2026-08-26 | Open |
| AD-06 | Confidence-weighting method for many-to-many `hs_to_industry` mappings | 2026-08-26 | Open |
| AD-07 | `run_id` naming/versioning convention for `model_input/` | 2026-08-26 | Open |
| AD-08 | Where feature engineering (YoY %, rolling windows) is computed — `processed` vs. `features` | 2026-08-26 | Open — leaning `features` |
| AD-09 | Formal schema-enforcement mechanism (library/tooling) vs. documentation-only `schemas/*.yaml` | 2026-08-26 | Open — deferred until implementation begins |

## Resolved

| ID | Decision | Resolution | Date |
|---|---|---|---|
| AD-R1 | File format per layer | CSV for `mapping/` (diffable, hand-curated); Parquet for `interim`/`processed`/`features`/`targets`/`model_input`; YAML for `config/`; DuckDB optional query layer only | 2026-08-26 |
| AD-R2 | How much temporal metadata to carry in `processed` vs. `interim` | Full 6-field set lives in `interim`; only `available_as_of` is mirrored per-variable into `processed`, since that is the only field the point-in-time cutoff logic queries directly | 2026-08-26 |
| AD-R3 | Revision-handling policy | Append-only `raw`/`interim`; `processed` built under exactly one explicit `vintage_policy` (`first_published` default for training features; `latest_revised` only for descriptive use, never mixed) | 2026-08-26 |
| AD-R4 | Train/validation/test split method | Strictly time-based (`prediction_date` ordered), never random-row; enforced by an explicit assertion after every split, not just by convention | 2026-08-26 |
| AD-R5 | Edition scope for the MVP core panel | **Current 2021-based OIE edition only.** Older editions are not bridged into the MVP panel. The A1/A2 bridge validation (`oie_overlap_bridge_v1`/`v2`) is preserved as research documentation and is not consumed by production code. Rationale: A2.1 approved a both-component bridge for only 1 of 11 industries, so a bridged panel would be unbalanced and mostly unapproved | 2026-08-27 |
| AD-R6 | Panel shape | **Balanced 12-industry monthly panel** — exactly 12 industries in every month, asserted in code and tests. A balanced panel keeps industry-relative normalization comparable across months | 2026-08-27 |
| AD-R7 | TSIC 18 and 33 | **Excluded from the target taxonomy.** Neither is reported as a division-total row in either verified current-edition workbook. Excluded rather than imputed. Whether OIE folds that activity into another category is unknown — no methodology document was located (`docs/task_a1_report.md` §4.2) | 2026-08-27 |
| AD-R8 | TSIC Division 12 (tobacco) — resolves AD-04 | **Assigned to IND-01**, renamed "Food, Beverages and Tobacco". Division 12 IS reported by the current OIE files; the earlier draft mapping omitted it entirely, which was a real coverage gap. Its within-industry weight is small (~3.4% of IND-01 in 2021-01) | 2026-08-27 |
| AD-R9 | Preliminary observations | **Retained in the panel, excluded from model-eligible output.** `is_model_eligible=false`, values kept intact, `data_quality_flag='preliminary'`. Deleting them would discard the most recent month; treating them as final would train on figures that will be revised | 2026-08-27 |
| AD-R10 | Historical bridging | **Deferred to Phase 2.** Blocked on the 5 `Needs source review` pairs from A2.1, which need an OIE methodology/revision note that has not been located | 2026-08-27 |
| AD-R11 | Evaluation framing / temporal metadata | **Latest-vintage historical data.** `release_date` and `available_as_of` are emitted NULL for every row because neither could be verified; no per-month publication date exists in the workbooks and no archived release calendar was found. Any future point-in-time work must obtain real vintage metadata first. This is a documented limitation, not a gap to be filled with an assumed lag | 2026-08-27 |
| AD-R12 | Draft taxonomy vs. production taxonomy | The **draft** mapping stays frozen inside `scripts/audit_target_sources.py` as `DRAFT_INDUSTRY_TSIC_DIVISIONS_A1A2_FROZEN` (aliased to its original name for the checksum-pinned bridge scripts). **Production** reads `configs/industry_mapping.yaml` only. Rationale: the preserved A1/A2 artifacts were computed against the draft and are SHA-256 pinned; silently re-pointing them at the new taxonomy would invalidate that evidence instead of superseding it honestly | 2026-08-27 |
| AD-R13 | Shared low-level parsing logic | Moved from `scripts/audit_target_sources.py` into `thai_supply_chain_ews.data.oie` — one production implementation. The audit scripts import it and re-export the original names; the package never imports the scripts. Verified by a compatibility test | 2026-08-27 |
| AD-R14 | MPI–CapU redundancy (Task B2) | **`redundant`** under the pre-registered gate `component_redundancy_gate_v1`: pooled \|Spearman\| on industry-relative ranks 0.8601 (bar 0.85), 10/12 industries \|rho\| >= 0.75 (bar 9), median Jaccard of top-20% stress months 0.6319 (bar 0.60). All three conditions met. Verdict is marginal — failing any one would have flipped it to `non_redundant` — and the thresholds were fixed before the data were seen and not moved | 2026-08-27 |
| AD-R15 | K=2 equal-weight composite | **`rejected`.** Equal weighting is only approved when both components pass semantics AND the pair is `non_redundant`; the pair is `redundant`. Both components DID pass semantics individually. This supersedes the project's earlier working assumption of a two-component equal-weight MVP index. Equal weighting was never fitted or optimized — it is a design choice applied after industry-relative normalization, not an estimated optimum | 2026-08-27 |
| AD-R16 | Component semantics (Task B2) | **`component_semantics_approved: true` for both MPI and CapU**, recorded as a field separate from `source_verified` and from `target_approved`. MPI adverse change is `-100*(mpi_t/mpi_t-12 - 1)` in percent; CapU is `-(capu_t - capu_t-12)` in PERCENTAGE POINTS, additive — never a relative percentage change | 2026-08-27 |
| AD-R17 | Diagnostic vs production normalization | B2's industry-relative percentile ranks are labelled `diagnostic_only_full_sample_rank` and are **not leakage-safe**. They must never enter the production panel or serve as model features; a test asserts the B1 panel contains no such column. Leakage-safe training-only / expanding-history normalization is deferred to Task B3 | 2026-08-27 |
| AD-R18 | Per-industry component policy | **Open, deliberately not decided in B2.** The project-wide verdict hides real heterogeneity: IND-02 and IND-03 have identical top-20% stress months (Jaccard 1.000), while IND-12 (rho 0.396, CI includes zero) and IND-07 (Jaccard 0.222) are clearly not duplicative. Choosing a per-industry policy after seeing these results would repeat the error the pre-registered gate exists to prevent | 2026-08-27 |
| AD-R19 | MVP target component selection (Task B3) | **MPI is the sole primary target component; K=1.** CapU is REMOVED from the composite target but stays in the B1 panel as an auxiliary indicator and possible lagged feature. Export and PPI are NOT replacement target components for the MVP (both remain `source_verified: false`); upstream indicators will be evaluated as PREDICTORS in a later phase, not as target components. Rationale: B2 classified MPI+CapU as `redundant`, so rather than retaining a duplicative second component — or reopening B2's preregistered thresholds to reverse its conclusion — the target is redefined around MPI alone | 2026-08-27 |
| AD-R20 | Target naming | Predicted outcome is the **Industry Production Stress Score** (คะแนนความเครียดด้านการผลิตของอุตสาหกรรม), raw measure `mpi_adverse_yoy`. Deliberately NOT called a "Supply Chain Stress Index": MPI measures the production CONSEQUENCE of a shock, not every dimension of supply-chain disruption. The system remains the Thai Supply Chain Shock Early Warning System, whose future INPUT FEATURES represent raw-material, energy, import, trading-partner and logistics shocks | 2026-08-27 |
| AD-R21 | Leakage-safe calibration | `IndustryStressCalibrator` with an explicit fit/transform split: industry-relative empirical CDF, `score = 100*(count(ref < x) + 0.5*count(ref == x))/n`, minimum 24 reference observations, fitted only on months at or before an explicitly declared training cutoff. `fit` RAISES on any post-cutoff observation rather than trimming. B2's `diagnostic_only_full_sample_rank` is explicitly not reused — it was full-sample by construction | 2026-08-27 |
| AD-R22 | Static labels vs fold-specific scores | RAW horizon labels are persisted (they need no fitted statistic); CALIBRATED scores are fold-specific and are NOT persisted as production outputs. Producing one full-sample calibrated score and calling it leakage-safe would collapse the fold boundary into a single leak | 2026-08-27 |
| AD-R23 | Risk-level thresholds | Preregistered MVP policy bands on the 0-100 score: Normal [0,70), Watch [70,85), High [85,95), Severe [95,100]. These are PRESENTATION outputs, not observed ground-truth classes. The primary ML task remains continuous prediction; thresholds must not be tuned on the final test period; class frequencies may be imbalanced by design | 2026-08-27 |
| AD-R24 | Walk-forward evaluation protocol (Task B4) | Development origins **2024-01..2025-03** (15 months, 180 out-of-fold rows per horizon); purge/model-selection buffer **2025-04..2025-06** with no metrics reported; locked final test **2025-07..2026-04** (h=1, 120 rows) and **2025-07..2026-02** (h=3, 96 rows) RESERVED and not evaluated. The buffer exists so every 3-month development window closes before the locked period begins | 2026-08-27 |
| AD-R25 | Locked-test protection | The B4 runner takes **no command-line arguments at all**, so no flag can accidentally evaluate the reserved period; `run_walk_forward` additionally calls `assert_not_locked_test` and raises `LockedTestAccessError`. Only a key/count/checksum manifest is emitted for the locked period. Unlocking is a separate explicit task | 2026-08-27 |
| AD-R26 | Per-origin refit | The calibrator is refitted and frozen at **every** monthly origin (15 distinct reference fingerprints), never once across development. Training labels are selected on `target_window_end <= origin`, per horizon — NOT on the label's origin date, which would admit 3-month windows reaching past t | 2026-08-27 |
| AD-R27 | Clustered uncertainty | The bootstrap resamples **whole origin months** (all 12 industries together) in 3-month moving blocks, 1,000 replications, seed 20260827. 180 industry-month rows are not 180 independent observations: industries share calendar shocks and YoY/3-month windows overlap. A CI crossing zero is reported as INCONCLUSIVE, never as evidence of equivalence | 2026-08-27 |
| AD-R28 | Development benchmarks selected | **h=1: `persistence_current_month`** (macro MAE 14.53, conclusively better than the runner-up). **h=3: `persistence_trailing_3m_max`** (macro MAE 14.70) — but its advantage over `persistence_current_month` is **NOT conclusive** (paired CI includes zero); it was chosen on the preregistered tie-break of fewer false negatives at score >= 85 (5 vs 12). Any future ML model must beat these on the same protocol | 2026-08-27 |
| AD-R29 | First upstream shock source family (Task C1) | **World Bank Pink Sheet** (`CMO-Historical-Data-Monthly.xlsx`) verified and ingested. The download URL is DISCOVERED from the official landing page each run, not hard-coded, because the World Bank rotates a document hash in the path monthly. Vintage July-2026, SHA-256 `7902a775...`, manifest tracked and idempotent by checksum | 2026-08-27 |
| AD-R30 | Commodity candidate statuses | **5 source_verified** (Brent, LNG Japan, Aluminum, Copper, Rubber RSS3); **2 conditional** — `coal_australia_usd_mt` (benchmark switches spot -> futures at Feb 2022) and `palm_oil_usd_mt` (three grade/delivery-basis regime changes: Jan 2021, Nov 2024, Feb 2025). Both breaks fall INSIDE the modelled window, so neither series is source-verified. Data quality itself is clean for all seven: 0% missingness, no duplicate months | 2026-08-27 |
| AD-R31 | Source verification is not feature approval | Three separate fields per candidate: `source_verified`, `feature_semantics_approved`, `model_feature_approved`. The latter two are **false for every candidate** after C1 — transformation and timing semantics were not evaluated. Collapsing these into one boolean is how a merely-downloadable series becomes a model input by accident | 2026-08-27 |
| AD-R32 | Commodity feature timing unresolved | `latest_vintage_only: true`, `point_in_time_backtest_supported: false`, `same_month_availability_verified: false`, `minimum_safe_lag_months: null`, `timing_status: unresolved`. `release_date` and `available_as_of` are null on every row and the download date is NOT used as a proxy for historical availability. **C2 must default to lagged use** unless stronger publication evidence is found. The workbook itself states recent LNG months are estimates, so revision risk is documented, not hypothetical | 2026-08-27 |
| AD-R33 | Brent cross-check not executed | FRED was unreachable from this environment (connection reset on three endpoints; `WinError 10054`), so the World Bank/FRED Brent comparison **did not run**. Recorded as `not_executed_source_unreachable` — a state deliberately distinct from the preregistered consistent/inconsistent/conditional classes, because reporting an unmeasured comparison as "conditional" would misrepresent a missing measurement as a middling result. The comparison logic is implemented and synthetically tested at all boundaries. FRED Brent must never become a second Brent feature | 2026-08-27 |
| AD-R34 | Pink Sheet vintage archive is effectively unavailable (C1.5) | The official monthly archive listing covers **2000-2013 only**; the landing page links the **two most recent issues**; document URLs embed an opaque per-release hash that cannot be derived. Three issues were obtained (June/July/August 2026), of which one was found by pattern-probing inside an already-discovered folder and is labelled `pattern_probe_within_discovered_folder`, never counted as discovery. **62 of 65 required reference months have no archived evidence, and zero archived issues cover the B4 development window** | 2026-08-27 |
| AD-R35 | Publication timing measured | Reference month t is first published **during month t+1**, 1-4 days after month end (2026-05 -> 2026-06-02, 2026-06 -> 2026-07-01, 2026-07 -> 2026-08-04). Issue dates are read from the PDF's own `/CreationDate`, never from the download timestamp. A sparse archive makes "earliest archived issue containing t" an UPPER BOUND, not a first release; upper-bound rows are excluded from delay statistics so the lag is not overstated | 2026-08-27 |
| AD-R36 | Same-reference-month use prohibited | **Structurally impossible, not merely unverified**: month t's completed value does not exist at any origin inside month t. `same_month_availability_verified: false`. The project's forecast contract is **calendar-boundary month-based** (per B4's own feature rule and B1's null release metadata), not an operational-release contract, and cannot be evaluated as one without verified MPI release dates | 2026-08-27 |
| AD-R37 | minimum_safe_lag_months stays NULL | Two blockers: (a) archived publication evidence covers none of the 15 B4 development origins; (b) **measured revisions beyond display rounding** mean a lag alone does not remove revision leakage from retrospective use of the latest workbook. A separate, clearly-labelled `recommended_conservative_lag_months: 2` is a POLICY choice for C2, not a verified measurement, and the two fields must not be conflated | 2026-08-27 |
| AD-R38 | Revisions measured; LNG excluded from the primary feature set | Comparing first-release PDF values against the latest workbook (after rounding to the PDF's displayed precision): **LNG Japan 2026-05 was first published at 15.77 and now reads 12.88 - a -18.33% revision**; 2026-06 -8.11%. Palm oil -0.35%. Brent, coal, aluminum, copper and rubber show no revision beyond rounding. `lng_japan_primary_feature_eligible: false`. A same-vintage comparison showing zero difference is classified `revision_status_unresolved`, never "unchanged" | 2026-08-27 |
| AD-R39 | **CORRECTION to AD-R34..AD-R38 (Task C1.5-R1)** | The archive conclusion in AD-R34 was WRONG. It rested on incomplete discovery: only the landing and report-archive pages were searched, and only the hyphenated filename form was matched, so a detail page's MAIN DOCUMENT href (`CMOPinkSheetFebruary2021.pdf`, squashed) was invisible. Parsing official document-detail pages and RELATED link lists yields **all 65 required issues, 0 missing** — 64 via `official_related_href`, 1 via `official_detail_main_document`, 0 by pattern probe. Consequences: **first-release values are reconstructed for all 65 required reference months and all 15 B4 development origins**, so archived point-in-time data IS available (AD-R34's contrary claim withdrawn); `minimum_safe_lag_months` is SPLIT into a verified **`minimum_publication_lag_months: 2`** (64 months at t+1, January 2025 at t+2 because the February 2025 issue skipped it) and a still-null `revision_safe_lag_months` — AD-R37 had let revision findings null the publication lag, conflating two different questions; `timing_status` moves from `partially_verified` to **`verified`**. Two parser defects were found and fixed: older issues use a `Commodity Unit ...` header row (31 issues failed to parse), and unpublished monthly cells printed as an ellipsis caused "last three numbers" to read QUARTERLY values, fabricating a spurious **+118% coal revision** that does not exist. Revisions re-measured on 65 months: LNG revised in **95.5%** (max 34.1%), palm oil 29.9%, coal 3.4%, copper 3.0%, aluminum 1.5%, **Brent and Rubber RSS3 never**. Unchanged and still valid: lag zero prohibited, same-reference-month use structurally impossible, calendar-boundary contract, latest-vintage is not point-in-time when lagged, LNG ineligible for the primary feature set, Coal/Palm oil definition breaks are semantic and both series stay excluded, `model_feature_approved` remains false | 2026-08-27 |
| AD-R40 | **CORRECTION to AD-R39 (Task C1.5-R2)** | AD-R39's February 2025 finding was WRONG, and with it the lag it produced. R1 reported that no distinct February 2025 issue exists and that the RELATED link served a PDF byte-identical to January. **Root cause:** R1 requested the `/original/` variant of the February anchor. Under `/en/doc/<hash>/original/` the World Bank serves the document family's MAIN document REGARDLESS of the filename in the path — proved directly, since `original/CMO-Pink-Sheet-NONSENSE-9999.pdf` returns the same bytes as `original/CMO-Pink-Sheet-June-2025.pdf`. That family's main document was January's. R1's href regex ranked `/related/` and `/original/` identically, so the first-seen `/original/` hit won and the correct `/related/` URL was discarded; nothing then compared the file against its own internal date or displayed columns, so a fetch defect became a published claim about the archive. **Corrected evidence:** February 2025 is distinct — 256,996 bytes, SHA-256 `94f053ef...`, `/CreationDate` 2025-02-04, newest column 2025-01; January 2025 is 257,605 bytes, `b53a6b98...`, 2025-01-03, newest column 2024-12. **Consequences:** 65 issues obtained with 65 DISTINCT checksums and **0 duplicates** (AD-R39's `duplicate_content_issues: [2025-02-01]` withdrawn); all **65 of 65** reference months are MEASURED with **0** upper-bound months, covering **15 of 15** B4 development origins; **`minimum_publication_lag_months` moves from 2 to 1**, since the 2 existed only because of the wrong February file and a lag must follow the evidence rather than preserve a pipeline defect as policy; `publication_timing_status` moves from `partially_verified` to **`verified`**; `archived_first_release_backtest_eligible` from false to **true**. `recommended_operational_lag_months` is **2** and remains POLICY, never a measured minimum. **Safeguards added:** every candidate href per issue is retained and ranked (`/related/` above `/original/`, which may never assert issue identity); each download must pass `validate_issue_identity` — internal creation date inside the expected issue month AND newest displayed column equal to that month minus one — with fall-through to the next candidate on failure; raw storage and cache keys use `{document_hash}__{filename}` rather than the basename; a SHA-256 shared by two different validated issue months now RAISES `IssueIdentityError` instead of being recorded as `duplicate_content`. **Revision statistics rescoped** to the 65-month required window (R1 counted 67, including the pre-window months 2020-11 and 2020-12 that the February 2021 issue displays; those are now reported separately — 14 observations, 0 revisions): LNG revised in **65/65** months (max 34.1%), palm oil 20/65, coal 2/57, copper 2/65, aluminum 1/65, **Brent and Rubber RSS3 never**. **Unchanged and still valid from AD-R39:** the discovery correction itself (entry points and the squashed filename form), both parser fixes, lag zero prohibited, same-reference-month use structurally impossible, calendar-boundary contract, latest-vintage is not point-in-time when lagged, LNG ineligible for the primary feature set, Coal/Palm oil definition breaks are semantic and both series stay excluded, and `model_feature_approved` remains **false** for every series | 2026-08-27 |
| AD-R41 | Commodity feature set and exclusions (Task C2) | **Exactly four primary series**: `brent_crude_usd_bbl`, `aluminum_usd_mt`, `copper_usd_mt`, `rubber_rss3_usd_kg`. **Three excluded, with NO substitutes introduced**: LNG Japan (first releases revised in 65/65 required months, up to 34.10%, C1.5-R2), Coal Australia (2022-02 spot->futures definition break inside the window), Palm oil (grade/delivery-basis changes at 2021-01, 2024-11, 2025-02). Replacing an excluded series would be a NEW SOURCE DECISION made without the C1 verification that justifies one, so C2 builds only from what was already cleared and leaves the gaps visible. Requesting an excluded series raises `ExcludedSeriesError`; a test asserts none appears in the canonical table | 2026-08-27 |
| AD-R42 | Five preregistered transformations, and no others (Task C2) | `price_level`; `log_change_1m_pct`, `log_change_3m_pct`, `log_change_12m_pct` = 100*(ln p_m - ln p_{m-k}) with NATURAL logs and 100x scaling (not log10, not a simple percentage change); `realized_volatility_3m_pct` = population std with **ddof=0** over r_{m-2}, r_{m-1}, r_m. ddof is preregistered because ddof=1 would inflate every value by sqrt(3/2) (~22%) on a 3-point window — a definitional choice, not a market property. Volatility declares **FOUR** input months, not three: r_{m-2} is itself a change from m-3, and understating the inputs would let the feature claim availability a month early. **Canonical total 1,224 rows** (306 x 4), derived from the declared lags rather than hard-coded, and the runner fails explicitly on any other count. No other window, rolling z-score, shock flag, moving average, interaction term or direction adjustment is created in C2 — industry exposure and sign are C3 | 2026-08-27 |
| AD-R43 | Availability is three separate months (Task C2) | `source_available_month` (when the observation was ACTUALLY published, from the archived issue's own internal date), `policy_available_month` (reference month + operational lag), and `feature_available_month` = **max(policy month, EVERY input's source-available month)**. Taking only the final month would let a 12-month change publish before one of its own inputs existed. `minimum_publication_lag_months: 1` is MEASURED (C1.5-R2); `operational_lag_months: 2` is POLICY — the measured lag plus one month of revision margin — and the two live in different fields so the margin can never be read back as a measurement. **Lag zero is prohibited structurally**: `AvailabilityPolicy` raises `LagZeroProhibitedError` at construction, so a zero-lag policy cannot exist to be used by accident, and a lag below the measured publication lag is rejected too. **Availability is never derived from the download date** — every archived issue here was downloaded in the same month, so that would collapse all 65 source months to one value; more than 50 distinct values appear. A lag-1 table exists on the same archived evidence, is labelled `sensitivity_only`, is never the default, and was not compared with any target; `availability_policy` is part of the canonical key so the two tables coexist without either shadowing the other | 2026-08-27 |
| AD-R44 | C2 approves construction, not usefulness | A new approval field, **`feature_construction_approved: true`**, sits between `timing_approved` (C1.5) and `feature_semantics_approved` (C3); `model_feature_approved` stays **false** for all four series, restated per series in config. C2 computed NO correlation with MPI, mutual information, Granger causality, feature importance, predictive performance, high-stress statistics, target distribution, industry sign or weight, and opened no locked-test outcome; the runner takes no command-line arguments and a test asserts it never imports the targets, evaluation or matrices packages. No feature was removed for looking weak against the target, because no such comparison exists to have informed a removal. Lineage is HASHED rather than asserted: each of the 1,224 rows carries a distinct SHA-256 over its ordered input tuples (series, month, displayed TEXT and value, issue date, issue checksum), so omitting an input or reordering months moves the digest. No imputation, forward-fill, interpolation, clipping, winsorization, or rounding of calculated features; missing inputs, non-positive prices and non-finite results each raise, and every guard is exercised against a deliberately broken fixture. **Incidental finding, C1.5 NOT modified**: C1.5's boolean `estimate_marker` is set by the token `a/`, which the archived PDFs' own footnotes define as "Included in the energy index" — an index-membership marker, NOT an estimate flag (confirmed in 2021 and 2026 issues). No C1.5 decision depends on it, since LNG's exclusion rests on its measured revision record, but the field name and the derived `lng_estimate_to_final_transitions` count are misleading. C2 carries the token through as `source_footnote_marker` without restating it as an estimate claim; correcting C1.5 is left as a separate explicit task | 2026-08-27 |
| AD-R45 | **CORRECTION to AD-R38..AD-R40 (Task C1.5-R3)** — footnote and estimate semantics | C1.5 set a boolean `estimate_marker` whenever a series row carried the token `a/`. **That token does not mean estimate.** The archived issues' own footnote block defines the single-letter tokens as INDEX MEMBERSHIP: `a/` = included in the energy index, `b/` = non-energy index, `c/` = precious-metals index, `d/` = index-definition footnote (confirmed in 2021 and 2026 issues). Index membership was read as data provenance, so Brent — an energy commodity — was labelled an estimate in all 65 issues, which the source never claims. The publisher is not even consistent about printing the tokens: the Rubber RSS3 row carries `b/` in **22** of 65 issues and omits it in **43**, so the marker is recorded exactly as displayed and never normalised. **Independent evidence:** estimate status comes from a Description statement — "Liquefied natural gas (Japan), LNG, import price, cif; recent two months' averages are estimates" — matched (not assumed) in **65 of 65** issues. Each observation is classified by position among its issue's own ordered monthly columns: the two newest displayed months are `source_documented_estimate`, older displayed months are `not_covered_by_current_estimate_rule`, series with no documented rule are `not_documented_as_estimate`, and an issue lacking the statement yields **`unknown`** — `false` is deliberately absent from the vocabulary, because an unparsed note means the status is unobserved, not that the value is final. **Fields:** `estimate_marker` is REPLACED by `source_footnote_marker` (raw token, verbatim), `source_footnote_meaning`, `estimate_status`, `estimate_evidence_type`, `estimate_evidence_text`, `estimate_rule_version`, `estimate_evidence_url`; the revision status value `first_release_estimate` is RENAMED `first_release_documented_estimate`. **Terminology:** `lng_estimate_to_final_transitions` asserted two unsupported things — that `a/` meant estimate, and that the latest vintage is FINAL. No Pink Sheet issue establishes finality. It is superseded by `documented_estimates_differing_from_latest_vintage` (**65**, re-derived from Description evidence, not retained because previously reported) and `estimates_with_a_later_non_estimated_vintage` (**63** — the actual transition count). `first_non_estimated_vintage` records only the first archived issue in which the two-month rule no longer covers a month; it is NOT a claim of permanent finality. **Right-censoring:** for month m the qualifying issue is m+3; the archive ends at the June 2026 issue, so **2026-04 and 2026-05 are right-censored** and are reported as unresolved rather than folded into either outcome (65 documented estimates = 63 resolved + 2 censored; of the 63, **63 were revised** and **0 unchanged** before that vintage). **UNCHANGED and re-verified identical:** every numerical revision result — LNG 65/65 max 34.10% median 4.01%, palm oil 20/65, coal 2/57, copper 2/65, aluminum 1/65, Brent and Rubber RSS3 zero — because those were always measured independently of any marker; also archived PDF values, publication dates, checksums, archive coverage (65/65), publication lag 1, and B1/B3/B4 artifacts. **LNG remains excluded** from the primary feature set on that revision record, unaffected by the corrected transition count. **C2 impact:** feature values, availability months, lineage checksums and row counts verified IDENTICAL across all 2,448 primary and sensitivity rows; C2 gains `source_footnote_meaning` and `estimate_status` columns and all four primary series are `not_documented_as_estimate`. The canonical table's CONTENT CHECKSUM changed (`c6f20be5...` -> `29e44c52...`) because two metadata columns were added and aluminum/copper now correctly report `b/` where the old boolean produced an empty string — a METADATA-ONLY checksum change, reported separately from a feature-value change, which did not occur | 2026-08-27 |
| AD-R46 | Official I/O source for the structural matrix (Task C3) | **NESDC Input-Output Table of Thailand 2015 (Final)**, 665,344 bytes, SHA-256 `7da4b4dca118af76...`, plus the official 'Input-Output Description' classification PDF (`87a65982fa12b643...`). URLs are DISCOVERED by walking NESDC's own sitemap (41 sections, 7,918 download URLs, 21 I/O pages) to each download page's `?p=&ddl=` redirect — never hard-coded, because NESDC migrated to WordPress and the historical `main.php?filename=io_page` now serves a page with ZERO I/O links (recorded as `legacy_io_page_still_lists_io_files: false`). Two transport quirks handled explicitly: a WAF cookie challenge (bounded retry) and a router that answers content pages with HTTP 404 while serving a full body — accepted ONLY for HTML, never for a file download, so a missing workbook cannot be mistaken for a served one. **The brief named a 58-sector workbook; no such file exists officially.** NESDC publishes a **180-sector** table for 2015; C3 uses what is published and records the discrepancy. A newer **2021 (180 Sectors)** table exists, was downloaded, checksummed and parsed, and is **structurally compatible** (identical header and code universe) — but is **audited, NOT substituted**: changing the structural reference year is an explicit decision, not a silent default, and its final/preliminary status is UNRESOLVED (the 2015 file self-titles '(Final)'; the 2021 file carries no such marker) | 2026-08-27 |
| AD-R47 | I/O extraction and coefficients (Task C3) | The workbook is a SPARSE LONG table (`ROW|COLUMN|PURCHASER|WHOLESALE|RETAIL|TRANSPORT|IMPORT`), not a matrix; the header is located by LABEL and the intermediate block by explicit sector-code membership, never by worksheet coordinates. **The subtotal trap is the central hazard**: columns 309/409/509 are subtotals of the codes above them and 190/600/700 are grand totals, so summing them alongside components double-counts and folding any into Z would put final demand, imports or value added inside the coefficients. Any code outside the documented universe RAISES rather than being guessed at. Valuation: **purchasers' prices, import-INCLUSIVE**, margins itemised, and import-specific coefficients ARE available (per-cell IMPORT field) so they are reported available rather than inferred. `PURCHASER` is the primary measure because world-price cost pressure reaches a producer regardless of whether the input was imported, and Thailand imports most crude, aluminum and copper — a domestic-only matrix would report near-zero crude exposure; the consequence is stated, that inverting an import-inclusive A gives technology-sense total requirements, NOT a domestic output multiplier. Validation: 0 duplicate cell keys, gross output == total-input row for all 180 sectors, column identity `Z + VA = X` residual **exactly 0**, column sums 0.0000-0.9017 (all < 1), 0 negative coefficients, condition number **7.839**, Leontief reconstruction residual **1.110e-15**. **Sector 179 has zero gross output** and is EXCLUDED, never divided by and never zero-filled — writing zero would assert it requires nothing. `T = L - I` removes the identity term, because a sector's own unit of output is not one of its own requirements. An ill-conditioned `(I-A)` STOPS rather than being regularized | 2026-08-27 |
| AD-R48 | Crosswalks and aggregation (Task C3) | **No commodity mapping is exact.** Brent -> **031 Petroleum and natural gas** (broader: prices two things, Brent prices one). Aluminum AND Copper -> **107 Non-ferrous metal**, the SAME sector, so their 24 rows are **identical by construction** — one aggregate exposure reported twice, NOT two independent measurements; all carry `shared_io_source_sector: true` and a test asserts the flag wherever coefficients coincide. Rubber RSS3 -> **095 Rubber sheet and block rubber**, one grade within the category but correctly DISTINGUISHED from 016 (latex), 096 (tyres) and 097 (fabricated goods). All 48 mappings are therefore **medium** confidence. The 12 industries reproduce configs/industry_mapping.yaml EXACTLY — the loader refuses to run if any id, name or TSIC division differs — covering **91 I/O sectors** with no sector serving two industries. Documented scope differences: 116 straddles TSIC 26/27-28 and dominates IND-10 at 79% of output (IND-09 and IND-10 marked medium); furniture is split by material (080 wooden, 109 metal, both TSIC 31 -> IND-12); 083 (TSIC 18) and 127 (TSIC 45) are assigned to no industry. **Aggregation uses OFFICIAL GROSS OUTPUT (col 600), never equal weights**: the direct formula `sum(Z)/sum(X)` is the EXACT aggregate coefficient, not a mean of ratios, so no weight is invented; the total-requirement formula IS a weighted mean and is labelled as one, because the inverse of an aggregate is not the aggregate of the inverse. Weights that do not sum to one raise | 2026-08-27 |
| AD-R49 | C3 approves structure, not usefulness | **48 canonical rows** (12 x 4), 48 unique keys, all resolved, `direct + indirect = total` to 1e-12. **18 OBSERVED ZEROS kept distinct from unresolved**: a zero means the industry genuinely bought none of that input in 2015 and carries `observed_zero_direct_exposure`; unresolved would stay NULL. Collapsing the two would turn 'we don't know' into 'there is no exposure'. Ranges: direct 0.000000-0.710250, indirect 0.000747-0.296583, total 0.000916-0.857110. Largest: IND-04 x Brent **0.710 direct / 0.857 total** (refineries buying crude — the sanity check the matrix had to pass); IND-05 x Brent **0.006 direct but 0.194 total** (chemicals reach crude through refineries, the case a direct-only view would miss); IND-08 x Brent **observed zero direct with 0.169 indirect** — zero direct is not zero exposure. **Interpretive caveat recorded prominently**: IND-12's 0.2096 'aluminum' exposure comes from Jewellery (74.7% of its output, coefficient 0.2685 on sector 107), where what flows out of 'Non-ferrous metal' is largely GOLD — a correct reading of the official table and a poor proxy for either LME price. Direction: **45 `cost_pressure` (sign +1)**, **3 `mixed_or_ambiguous` (sign NULL)** where the industry contains the commodity's own sector and is thus both producer and consumer (IND-06 x Rubber, IND-08 x Aluminum, IND-08 x Copper) — the sign is NOT forced, because a wrong sign is worse than an absent one. The sign is a DOMAIN direction, never an estimated target sign. **No normalization across the four commodities** — a validator raises if any industry's four direct exposures sum to 1. Primary (direct A) and secondary (total requirements) matrices are labelled separately and neither was selected on target outcomes. Nothing was joined to MPI, no association computed, no weight tuned on an outcome, no model trained, locked test untouched, `model_feature_approved` **false** on all 48 rows. Limitations carried forward: the **2015 reference year** applied to a 2021-2026 window under an explicit `time_invariant_assumption`, sector aggregation, fixed proportions, and a 2021-edition classification document applied to the 2015 table (code universe verified, definitional identity NOT) | 2026-08-27 |
| AD-R50 | **CORRECTION to AD-R46 (Task C3-R1)** — the 58-sector workbook, and a commodity-proxy gate | **AD-R46 said the brief's 58-sector workbook does not exist officially. It does.** `DataIO2015x58.xlsx`, 239,507 bytes, SHA-256 `67c921a83fae15ed...`, sheet `DataIO2015x58`, internal title 'INPUT - OUTPUT TABLE OF THAILAND 2015 (58 Sectors)'. **Root cause:** discovery was scoped to ONE mechanism — sitemap -> /download/ pages -> `?p=&ddl=` redirects. The aggregated workbooks (x58/x26/x16) live in the WordPress MEDIA LIBRARY and are linked from no /download/ page; the 2015 page carries exactly ONE ddl link, so that path could never have found them. Contributing: `_DOWNLOAD_SLUG` matched only /download/ sitemap entries; the caller took `matching[0]`, the first Excel link per page (a LATENT defect — not operative here, but it would hide a second workbook on any page listing two); no filename search for the published `x58`/`x26`/`x16` suffixes; the media REST API was never queried. **Ruled out:** WAF cookie handling, same-URL redirect handling and 404-with-body handling were all correct and are NOT causes (the file downloads on the first attempt). **The error that mattered** is the same as C1.5-R1's February 2025 verdict: 'my one mechanism did not find it' was written down as 'it does not exist officially'. **Resolution:** the 180-sector table REMAINS PRIMARY (finer resolution is what makes the 12-industry crosswalk possible — 58 sectors cannot separate wooden from metal furniture, or refineries from other petroleum products); the 58-sector table is classified `official_aggregation_validation_source` and was NOT substituted automatically. `Book_IO2015_EN.pdf` also resolves the unit C3 left open: **thousand Baht**. **Reconciliation:** **12 of 12 partition-invariant grand totals match EXACTLY** (max absolute difference 0) at a tolerance of 0.5 preregistered from source precision — so the 180-sector calculations are CONFIRMED and the exposure matrix remains approved. Sector-level comparison is **`not_executed_official_crosswalk_unavailable`**: no official 180-to-58 concordance exists in the classification PDF (0 mentions of '58'), the 455-page official publication, or the media library, and the contiguous-partition hypothesis FAILS (58:003 == 180:004, skipping 180:003). C3-R1 REFUSES to infer the mapping by subset-sum fitting: fitting is not evidence and the solution is not unique. NOT EXECUTED is a third state, distinct from passed and failed. **2021 status: UNRESOLVED** — the 2015 file marks itself final twice (internal title '(Final)' and sheet 'Data IO2015 Final'), the 2021 file carries no marker in either place; finality is NOT inferred from structural compatibility, and it stays audit-only. **Proxy gate:** C3 marked all 48 pairs 'resolved, medium confidence', conflating coefficient availability with commodity-proxy fitness. Eight fields now separate them, with the verdict derived from each industry's dominant purchasing component checked against that component's OFFICIAL DEFINITION. Results: **2 `fit_for_structural_use`** (IND-04 x Brent, 100% via refineries where crude is the defining input; IND-06 x Rubber, 68.5% via tyres), **44 `broad_proxy_use_with_caution`**, **2 `not_fit_for_commodity_specific_use`** (IND-12 x Aluminum and x Copper — 95.7% of IND-12's sector-107 purchases are made by Jewellery, whose official definition names 'precious metals... silver, gold'), **43 of 48 feature-eligible**. The 5 ineligible fail for TWO DIFFERENT reasons that stay distinct: IND-12 x2 on PROXY FITNESS (number fine, meaning wrong); IND-06 x Rubber and IND-08 x Aluminum/Copper on DIRECTION (meaning fine, sign genuinely ambiguous because the industry contains the commodity's own sector). **Every ineligible pair KEEPS its coefficient** — eligibility is a usage decision, and zeroing or deleting would make 'not suitable' indistinguishable from 'no exposure'; validators raise on a dropped coefficient, a sub-48-row table, eligibility granted past contrary evidence, and a shared sector without its double-counting flag. **24 rows** carry `shared_io_source_sector` and `double_counting_risk`: aluminum and copper read the SAME sector-107 coefficient, are NOT independently measured, must not be summed or averaged, and may enter an aggregate shock index AT MOST ONCE. **Preregistered C4 policy:** direct exposure primary, total-requirement sensitivity-only, default membership `industry_pair_feature_eligible: true` only, ambiguous direction stays null, forced conversions prohibited. **NO C3 number changed** — all 48 exposures, directions and signs verified byte-identical; the gate adds fields only. C4 was not created, no target was read, no locked-test outcome was opened | 2026-08-27 |
| AD-R51 | Structural availability is a PUBLICATION date, not a reference year (Task C4) | The I/O table's reference year is **2015**; that is what it DESCRIBES. Backdating the matrix to 2015 would hand every 2024 forecast origin four years of structure nobody could read, and because the structure is CONSTANT the leak would leave no trace in any output — the most dangerous kind. The download timestamp is equally wrong in the other direction: these files were re-hosted during NESDC's 2025 site migration, so using it would make the matrix look unavailable throughout development. C4 uses the publisher's own stated release date instead: the official download page reads `31 มี.ค. 2563` = **2020-03-31**, and the Buddhist-era conversion is VERIFIED against a known case (the same field on the 2021 page reads 25 ก.พ. 2568 = 2025-02-25, matching that table's known release and its 2025/03 upload path). Status **`verified_before_development`**: 2020-03-31 precedes the first B4 origin 2024-01, so the primary matrix proceeds. Two guards: `StructuralAvailability` RAISES if the availability date is not after the reference year, and `assert_supports_primary` raises if the structure post-dates the first origin. One caveat kept explicit — what is verified is that the 2015 TABLE was published by then; the BYTES now held were re-hosted in 2025, so `byte_identity_verified_at_that_date: false`. And **`fully_real_time: false`** regardless, because the MPI target is still latest-vintage | 2026-08-27 |
| AD-R52 | Conditioned matrix: three variants, 39,474 rows (Task C4) | `conditioned = base_feature_value x exposure x price_increase_to_stress_sign`, over the **43 eligible** pairs only: **43 x 306 = 13,158 rows per variant**, **x 3 = 39,474**. Exactly three variants, each varying EXACTLY ONE assumption from the primary: `primary_direct_lag2` (lag 2, direct), `timing_sensitivity_direct_lag1` (lag 1), `structural_sensitivity_total_lag2` (total requirements). **No combined lag-1 + total-requirement variant exists and none can be constructed** — `MatrixVariant` counts the varied assumptions and raises on anything but one, because changing two at once produces a difference nobody can attribute to either. Availability is `max(base_feature_available_month, structural_available_by)` — a feature built from two things is only as available as the slower one — and future-dated input raises. Units are named honestly (`percentage_point_log_change_x_exposure_coefficient`): this is a **structural interaction feature**, NOT a monetary cost, price elasticity, causal effect or forecast coefficient, and multiplying a log change by an I/O coefficient does not make it money. No normalization across commodities or industries. Combined lineage binds the C2 checksum, the C3 exposure-row checksum and the structural source checksum; an incomplete lineage RAISES rather than emitting an untraceable feature, and all **39,474 lineages were re-derived independently and matched** | 2026-08-27 |
| AD-R53 | Six kinds of absence, and the five dark pairs (Task C4) | The five pairs C3-R1 excluded (IND-12 x Aluminum/Copper on PROXY FITNESS; IND-06 x Rubber and IND-08 x Aluminum/Copper on AMBIGUOUS DIRECTION) generate **zero** conditioned features, **keep their structural coefficients**, are tracked in a separate exclusion audit, and are never zero-filled — zero is a real exposure value here, so filling would make a structural exclusion indistinguishable from an industry that genuinely uses none of the input. A validator raises if a row for an excluded pair ever appears, and a synthetic test fabricates 306 such rows to prove it fires. **Six distinct states** are kept apart: `zero_due_to_exposure` (11,000 rows — the industry uses none of it), `zero_due_to_base_feature` (77 — the price did not move), `zero_due_to_both` (16), `not_generated_due_to_ineligibility`, `no_observation_available_at_origin`, and `insufficient_feature_history`. Collapsing any two would make an absence of knowledge look like a measurement. **A null direction sign is never inferred**: an eligible pair carrying one raises, and forcing a `mixed_or_ambiguous` pair to +1 is caught by the cost_pressure check | 2026-08-27 |
| AD-R54 | Development snapshot uses availability metadata, never positional shifting (Task C4) | **15 origins (2024-01..2025-03) x 12 industries = 180 rows**, 20 commodity-feature columns, **3,225 eligible cells** (43 x 5 x 15) and **375 structurally unavailable** (5 x 5 x 15), 3,600 total. Each cell is the latest reference month with `conditioned_available_month <= forecast_origin_month`. **Positional shifting is prohibited**: a fixed shift agrees with the availability rule only while publication stays regular and diverges SILENTLY when it does not — and this project measured the publication lag precisely so it would never have to guess. Ineligible cells stay **null with an explicit mask**, never zero; observed-zero exposures remain numeric zero. No target column is attached. **Aluminum/Copper**: 18,360 rows carry `shared_exposure_group: io_sector_107` and `additive_aggregation_allowed: false`; both price signals are kept because they are genuinely different prices, but `assert_no_duplicate_shared_sector_aggregate` rejects any aggregate containing that one shared coefficient twice, and the runner EXERCISES the guard on real rows so it is demonstrated rather than merely present. No combined non-ferrous index, PCA or weighted-average replacement. Nothing was joined to a target, no association computed, no feature selected on an outcome, no model trained, locked test untouched, `model_feature_approved` **false** on all 39,474 rows | 2026-08-27 |
| AD-R55 | Residual-correction design and two separate temporal cutoffs (Task D1) | The model predicts what the benchmark MISSES: `r = y - b`, `y_hat = b + r_hat`, one ridge per (outer origin, horizon, industry), **coefficients never pooled across industries**. The residual framing makes the benchmark the floor BY CONSTRUCTION — a model that learns nothing predicts r_hat = 0 and reproduces persistence exactly — so the comparison asks only whether commodity information adds anything BEYOND persistence. Benchmarks come from B4's own implementation, reused not copied, and reproduction is asserted against B4 production predictions. **Two cutoffs enforced separately**: (a) a label enters training only when `target_window_end <= t`, per horizon — the rejected alternative `training_forecast_origin < t` would admit a 3-month window still OPEN at t (at 2024-01 h=3 the correct rule permits origins to 2023-10, the naive one to 2023-12); (b) the feature vector for historical origin u is built from `conditioned_available_month <= u`, NOT <= t — refreshing it with what is known at t would make the row carry information published after the moment it claims to represent, so the model would learn from a version of history that never existed. They take different arguments so they cannot drift together, and both have synthetic failure tests. The target at t is read only after features, calibrator, hyperparameters, scaler, model and prediction are all frozen. Feature history is GATED, never imputed: first complete registered origin **2022-03**, and excluded rows carry `insufficient_registered_feature_history`. Usable outer-training counts reproduce the brief exactly — h=1 264/432 and h=3 240/408 at the first/last origin, against 288/456 and 264/432 label-permitted | 2026-08-27 |
| AD-R56 | The calibrator minimum binds earlier than the brief assumed (Task D1) | The brief expects earliest inner validation origins of **2023-03** (h=1) and **2023-05** (h=3), giving >=10 and >=6 inner months at the first outer origin. Those dates follow from the FEATURE gate alone (2022-03 + 12 origins) and ignore the CALIBRATOR's own requirement. B3's monthly stress series begins 2022-01, so the 24th observation falls at **2023-12** — the first month a calibrator can be fitted at all. Since D1 must preserve the 24-observation minimum, the earliest feasible inner validation origin is 2023-12 for BOTH horizons, leaving **1** inner month for h=1 and **0** for h=3 at the first outer origin. **The guard was kept and the expectation reported as UNMET**, rather than weakening a validity guard to reach a date. Preregistered fallback where fewer than 3 inner origins exist: **alpha 100.0** (most regularized) and **channel none** (fewest predictors), used at 6 of 30 origin-horizons and recorded per origin. The direction of that fallback matters: it shrinks the residual toward zero and so pulls the model TOWARD the benchmark, meaning it can only make the model look worse, never better — it cannot manufacture apparent skill where evidence is thinnest | 2026-08-27 |
| AD-R57 | **NEGATIVE RESULT: commodity features do not beat persistence on development (Task D1)** | h=1 macro MAE **15.3073 vs 14.5287**; h=3 **16.1072 vs 14.6985**. Skill **-5.4%** and **-9.6%**. Paired 95% CI **[0.4932, 1.1678]** and **[0.7319, 1.9405]** — both exclude zero, in the BENCHMARK's favour, since a positive difference means worse. Only **2 of 15** origins favoured the model at each horizon. Under the preregistered rule both horizons are **`incremental_signal_not_supported`**. Macro and pooled MAE are identical because the development panel is balanced (15 x 12, no gaps); they are still reported separately, because that identity is a property of this panel and not of the metrics. The nested selection agreed INDEPENDENTLY: **alpha 100.0, the most regularized value in the grid, was chosen at 27 of 30 origin-horizons** — the procedure was shrinking the commodity correction as close to zero as the grid allowed. **One asymmetry recorded, not used**: the model catches MORE high-stress events (recall 0.70 vs 0.65 at h=1; 0.90 vs 0.83 at h=3; 6 vs 7 and 3 vs 5 false negatives) by predicting more events at worse precision (0.52 vs 0.62; 0.62 vs 0.65). The preregistered rule says high-stress metrics QUALIFY the discussion but never override the MAE verdict, and the rule was fixed before those numbers existed. Both sensitivities were also worse than the primary (timing 15.60/16.13, structural 15.72/16.12) and by design could not have rescued it. **The result was NOT repaired** by widening the grid, adding transformations, or re-searching features. What it does NOT establish: that commodity prices are irrelevant to Thai industrial production — the chain tested is narrow (four broad proxies, static 2015 structure, 15 effective months, latest-vintage target) | 2026-08-27 |
| AD-R58 | Locked test stays closed regardless of outcome (Task D1) | `approved_for_locked_test` **false** at both horizons, no C2/C3/C4 feature marked model-approved, locked test **unopened**. Only the locked-test ORIGIN KEYS were read, for a non-overlap assertion; no locked target row or metric was read, joined, summarised or hashed. The runner takes NO arguments and no environment variable — the origin list comes from the development split and every origin is asserted through `assert_not_locked_test`. Purge-buffer origins (2025-04..06) appear in no D1 table. **1,080 predictions** (3 variants x 2 horizons x 15 origins x 12 industries), keys unique. Aluminum and copper NEVER coexist in a fitted model — asserted on all 1,080 — and the channel is chosen globally per origin-horizon rather than industry by industry, so one structural assumption is not varied twelve ways to flatter the score; copper was selected 18 times, aluminum never. Preprocessing: ineligible predictors OMITTED not zero-filled, zero-variance detected from X only (a different outcome leaves the same predictor dropped, asserted by test), scaler fitted on training rows only. Sensitivities reuse the primary alpha and channel, are reported separately, are not pooled with the primary, and cannot change its status | 2026-08-27 |
| AD-R59 | **OIE publishes reference month t during month t+1 — B4's current-month assumption is violated (Task D2)** | Across **111 months with credible first-publication evidence (2017-04..2026-07)** the lag from the end of reference month t to its first publication is **min 23, median 29, max 66 days** — always after month t has ended. Corroborated by a **directly observed release**: the live workbook was re-fetched on 2026-08-28 and had gained exactly one reference month (2026-07) versus the 2026-08-26 snapshot, with `Last-Modified` moving 2026-07-27 -> 2026-08-27 and the checksum changing. So `availability_lag_months = 1` and `same_month_availability_supported = false`. B4 assumed current-month stress is known at forecast origin t; at origin t the most recent published month is actually **t-1**. Status recorded as **violated**, affecting B4 baselines and D1 feature assembly. **The B4 split is NOT changed and no model is rerun** — D2 is an audit and the remedy is a separate, deliberate decision | 2026-08-28 |
| AD-R60 | A listing timestamp is not a publication date (Task D2) | The i-Index archive listing timestamp is a content-management field that moves when a page is edited or migrated, and **37 of 127 entries sit in shared-timestamp batches** (2017-06-13 x16, 2023-07-27 x15, plus three pairs). Better evidence is the attached PDF, cross-checked two ways: its HTTP `Last-Modified` and the upload stamp encoded in its own filename (Bangkok local, converted at +7). Where the listing is *later* than a corroborated attachment the entry was re-hosted and the attachment wins — June 2022 is listed 2023-07-27 but its attachment is dated 2022-08-01. Seven **distinct** evidence classes are recorded, not a confidence scale: `attachment_corroborated` 80, `rehosted_listing_later_than_attachment` 21, `attachment_last_modified_only` 9, `migration_batch_contaminated` 15, `listing_timestamp_only` 1, `directly_observed_release` 1. The 2016-01..2017-03 block retains **no** surviving first-publication evidence. None of these is a publication date STATED by OIE; all are supporting metadata, and the acquisition timestamp is never used | 2026-08-28 |
| AD-R61 | July has two spellings in the archive; the variant is accepted (Task D2) | Five of 127 entry titles spell July **กรกฏาคม** (with ฏ) instead of the standard **กรกฎาคม** (ฎ). Accepting only the standard spelling drops those five months and reports coverage gaps that do not exist. Both spellings map to month 7. With the variant accepted the archive is **127 entries, 127 distinct reference months, 2016-01..2026-07, contiguous with zero gaps**. Recorded because it is the same failure shape as C1.5-R1 and C3-R1: a parsing limitation presenting itself as a fact about the source | 2026-08-28 |
| AD-R62 | **No complete 12-industry MPI vintage was found (Task D2)** | Archive attachments for two pre-cutoff months (2025-06, 2022-06) contain **0 TSIC division codes paired with values**, no tabular sector breakdown, and name only 4-6 of 13 industry keywords — a *different* subset each month, i.e. selected highlights, not a series. The live workbook is a **single URL overwritten in place**; the two snapshots differ in checksum and the earlier vintage is not retained. The data.go.th OIE records (`gdpublish-43_011`, `gdpublish-43_02`) point at those same overwritten URLs. Per-month data.go.th MPI files published by the Electrical and Electronics Institute contain **electrical/electronics product rows only**, not the national index, and 2019-2023 was uploaded in bulk back-fill batches. Scope is stated explicitly: this is a statement about the mechanisms exercised (archive walk, detail pages, attachments, live workbook, CKAN search), **not proof that no vintage archive exists anywhere** | 2026-08-28 |
| AD-R63 | The workbook marks its newest month but defines no legend — meaning not asserted (Task D2) | The live workbook labels exactly one month with a trailing glyph (`มิ.ย.*`), and across the observed release the glyph moved from 2026-06 to 2026-07 while 2026-06 kept a value. The closed 2011- and 2016-based editions carry no glyph. **No legend for the marker appears anywhere in the workbook**, and the two archive PDFs inspected contain no `เบื้องต้น` (preliminary) statement. So `marker_semantics_documented: false`: the behaviour is recorded as observed, and the marker is NOT asserted to mean preliminary. Same discipline as C1.5-R3, where footnote tokens were wrongly read as estimate flags | 2026-08-28 |
| AD-R64 | **Base-year editions are re-estimated, not rebased — no splice is admissible (Task D2)** | Preregistered gate: two editions may be spliced only if, over their shared months, the newer/older ratio is constant per division (CV < 0.5%) and the division sets are identical. Measured on the **36 shared months (2021-01..2023-12, all pre-cutoff)** of the 2016- and 2021-based editions: only **2 of 21** divisions are a pure rescaling; **19 of 21 are re-estimated**, median ratio CV **3.12%**, max **6.45%**. Additionally TSIC division **16 is absent from both older editions** while IND-03 requires 16+17, and the 2011-based edition uses **ISIC** labels where the others use TSIC. `splice_admissible: false`, `silent_splicing_permitted: false`. A constant-factor splice would fabricate history OIE never published | 2026-08-28 |
| AD-R65 | **Historical extension is infeasible; the D1 fallback cannot be remedied this way (Task D2)** | D1 fell back on 6 origins because too little pre-origin calibration history existed; extending MPI history was the candidate remedy. It is not available. The primary 2021-based edition starts **2021-01**, after the required **2020-04**, so reaching the requirement means joining an older edition — and every join route fails AD-R64's gate. `latest_vintage_extension_feasible: false`, `point_in_time_extension_feasible: false`, `d1_fallback_remedied: false`. The gate was **not** weakened to make D1 rerunnable; that is forbidden by config (`may_weaken_gate_to_make_d1_rerunnable: false`) and asserted by test | 2026-08-28 |
| AD-R66 | **CORRECTION: the D1 fallback's effect direction is unknown, not guaranteed adverse (Task D2)** | D1 reporting (docs/d1_development_results.md section 7 and docs/d1_modeling_protocol.md section 4) stated that the fallback configuration `alpha=100`, `nonferrous_channel=none` "can only make the model look worse, never better". **That claim is withdrawn as unsupported.** Ridge at alpha=100 still fits an intercept and can retain the Brent and Rubber predictors, so heavy shrinkage biases predictions toward the benchmark without guaranteeing a direction for the resulting error. Recorded as `fallback_strongly_shrinks_toward_benchmark: true`, `fallback_guaranteed_to_worsen_model: false`, `fallback_effect_direction: unknown`. The D1 headline is unaffected: `d1_primary_result: incremental_signal_not_supported`, `d1_nested_protocol_fully_executed: false`, `d1_fallback_count: 6`, `d1_protocol_deviation_reason: insufficient_pre_origin_calibration_history`, `d1_approved_for_locked_test: false`. **AD-R39..AD-R58 are preserved byte-for-byte** — this correction is a new entry, not an edit to the historical record | 2026-08-28 |
| AD-R67 | **D2 evidence outcome: `timing_only_supported`; locked test stays closed (Task D2)** | Of the four preregistered outcomes, exactly one is assigned. Publication timing **is** established authentically (AD-R59/R60), and the required window **2020-04..2025-06 is 63 of 63 months covered** by credible evidence. But complete 12-industry point-in-time vintages do not exist (AD-R62) and history cannot be honestly extended (AD-R64/R65). Therefore a future target reconstruction **cannot** be described as point-in-time. Restrictions held throughout and are asserted by test: B3 targets not rebuilt, D1 not rerun, B4 split unchanged, no target-feature association computed, nothing tuned, **locked test unopened**, and no numerical MPI value read for `reference_month >= 2025-07` — enforced by `GuardedMonthlyReader`, which raises `NumericalCutoffError` rather than returning a number. Nothing was committed | 2026-08-28 |
| AD-R68 | **B4 and D1 are relabelled non-operational; D3 is a new contract, not an overwrite (Task D3)** | B4 evaluated at forecast origin t using stress month t, which AD-R59 established OIE does not publish until month t+1. Rather than editing B4 or D1, both are **preserved and remain fully reproducible** as `non_operational_calendar_boundary_experiment`, and D3 adds a new versioned contract `operational_contract_v1` alongside them. No B4 or D1 output file is overwritten; B3 raw target windows and raw values are reused **unchanged**; the development, purge and locked date ranges are untouched. Rewriting the old experiment would have destroyed the evidence that the defect existed | 2026-08-28 |
| AD-R69 | **Operational forecast origin: issue at t, observe t-1, predict t+1 (Task D3)** | At forecast issue month t the forecast is issued **after** the verified OIE release of reference month t-1; `latest_available_stress_month = t-1`; h=1 targets t+1; h=3 targets max stress over t+1..t+3. It is explicitly **not a nowcast of month t** - month t is unobserved at issue time and is skipped. Seven concepts are kept distinct in code and schema (reference month, publication date, publication month, forecast issue month, latest observable stress month, target month/window, target availability month) because collapsing any two is exactly how the original error entered. All **15** development issue dates are taken from the D2 release inventory, are credible-classified, and fall inside their own issue month; lags **25-30 days**. Migration-batch months are not eligible as issue dates, asserted by test | 2026-08-28 |
| AD-R70 | **Three information cutoffs moved together, enforced in three separate places (Task D3)** | (1) stress map `reference_month <= t-1`; (2) calibrator `training_cutoff_month = t-1`, refitted fresh at every origin and frozen before any prediction; (3) training labels `target_available_month <= t` where `target_available_month = target_window_end + 1 month`. The third **replaces B4's `target_window_end <= t`**, which admitted a label whose final MPI observation had not yet been published - exactly 12 rows per origin at h=1. Verified counts: h=1 **276** at 2024-01 and **444** at 2025-03; h=3 **252** and **420**; calibration **24** observations per industry at 2024-01 (reference end 2023-12) rising to **38** at 2025-03 (2025-02). 15 origins produce 15 distinct calibrator fingerprints. Violations **raise** rather than being filtered away, so a silently-dropped label cannot masquerade as one that never existed | 2026-08-28 |
| AD-R71 | B3 raw targets reused unchanged; B3 calibrated scores deliberately not reused (Task D3) | The raw future target windows are identical to B3 - h=1 `y=S_{t+1}`, h=3 `y=max(S_{t+1},S_{t+2},S_{t+3})` - and the raw values are reused byte-for-byte, asserted by a merge test against the B3 label table. The **calibrated** scores are recomputed, because the D3 calibrator stops at t-1 whereas B3's ran through t; reusing B3's scores would have carried an unpublished month into the scale after the whole point of the exercise was to remove it. One visible consequence: observed high-stress event counts shift slightly (h=1 **21** vs B4's 20, h=3 **28** vs 29) purely because the calibration scale changed, not because any target value changed | 2026-08-28 |
| AD-R72 | **Operational benchmarks and the operational penalty (Task D3)** | Five operational baselines, none tuned, blended or weight-averaged; h=1 carries 4 and h=3 carries 5 (the latest-published persistence runs alongside the trailing-3m one). **1,620** predictions (720 h=1 + 900 h=3), 180 per baseline-horizon, 12 industries per origin-horizon-baseline, unique keys, deterministic order, no purge or locked origin. Selected on development data only by lowest macro industry MAE: h=1 **`operational_persistence_latest_published`** at **17.0945**, h=3 **`operational_persistence_trailing_3m_max`** at **16.6338**. Operational penalty vs B4's selected benchmarks: **+2.5658** (h=1) and **+1.9353** (h=3). **This is not attributed to publication lag alone** - the calibrator cutoff and the label-availability rule changed in the same step, so the penalty is a joint effect of all three. B4's numbers appear only as a historical non-operational reference and were not used to select anything | 2026-08-28 |
| AD-R73 | **Timing validity and vintage validity stay separate booleans (Task D3)** | Contract status **`operational_month_timing_supported`**: all development issue dates verified, every input obeys release timing, no same-month stress used, label availability enforced. Reported **separately and never merged**: `point_in_time_values_supported: false`, `latest_vintage_evaluation: true`, `fully_real_time_backtest: false`. The framing is `release_lag_aware_latest_vintage_evaluation`. Honest timing is not point-in-time data: D2 (AD-R62) found no complete 12-industry MPI vintages exist, so the target VALUES remain latest-vintage. A test asserts the two flags can never both read true | 2026-08-28 |
| AD-R74 | The D1 correction moves to a sidecar erratum and the generated docs are restored (Task D3) | D2 inserted a correction blockquote directly into two **generated** D1 documents. That was the wrong home: regenerating either file would silently erase the correction. D3 restores both byte-for-byte (removing 872 bytes/13 lines and 592 bytes/9 lines respectively, leaving the original wording intact) and relocates the correction to `docs/d1_development_results.errata.md`, linked from the README, methodology, the D2 decision document and this log. **Provenance caveat recorded honestly:** the pre-D2 checksum snapshot was deleted at the end of D2, so restoration could not be verified against a stored digest; it is verified instead by exact removal of the known inserted blocks, by the absence of any D2 text, and by the presence of the original claim. The restored digests are now pinned in `docs/d3_restored_d1_checksums.json` so any future drift is detectable. AD-R39..AD-R67 preserved byte-for-byte | 2026-08-28 |
| AD-R75 | **Locked final test unreachable by construction, not by filtering (Task D3)** | Locked months are rejected inside `build_origin_contracts` **before any data is read**, so no end-stage filter is load-bearing. The runner exposes no flag, environment variable, function argument or alternate code path that reaches a locked origin - asserted by tests that inspect the signature and the source for `os.environ`, `getenv`, `--locked` and `locked_test_origins`. `docs/d3_locked_test_manifest.json` is **key-only**: it carries reserved month keys and no observed value, prediction or metric, asserted by test. Purge months (2025-04..06) produce no metric. `locked_test_evaluated: false`, `locked_test_outcomes_read: false`. The locked final test remains **unopened** | 2026-08-28 |
| AD-R76 | **CORRECTION to the D3 verification report (Task D3-R1)** | D3 reported that its own files passed Ruff and that the repository's remaining **79** violations were **pre-existing**. That second claim was **false and unverified**. Measured with `ruff check .` (ruff **0.16.4**, config `pyproject.toml`, `select = [E, F, I, UP, B]`, no exclusions, 98 files in scope), **all 79 violations were in the seven files Task D2 created and never linted**: `audit_d2_oie_mpi_timing.py` 45, `oie_mpi_history.py` 11, `oie_mpi_release_inventory.py` 9, `test_d2_oie_mpi_timing.py` 6, `target_timing_contract.py` 5, `oie_mpi_vintages.py` 2, `oie_mpi_discovery.py` 1. Zero were pre-existing, which is why earlier tasks could truthfully report a clean repository. The label **pre-existing** was applied without a snapshot, checksum or recorded command output to support it — the same evidence failure the project has corrected before. All 79 are now fixed (62 UP031, 6 E501, 5 UP017, 3 I001, 1 F401, 1 F841, 1 UP041): `ruff check .` reports **All checks passed**, with **no** configuration weakening, **no** added exclusion, **no** disabled rule family and **no** mass `noqa` — asserted by tests that run the full-repository command and inspect the config | 2026-08-28 |
| AD-R77 | Lint fixes were behaviour-preserving, verified by rerunning D2 and D3 (Task D3-R1) | Every fix was text-preserving: a `%`-format became an f-string rendering the identical string, and long lines were split with implicit concatenation that concatenates back to the same characters. D2 source modules changed, so **D2 was rerun** and its output compared field by field ignoring only acquisition timestamps — the audit JSON and timing-decision JSON are **semantically identical**, including every blocker string, evidence-class count and lag statistic. **D3 was rerun twice**: predictions, metrics, benchmark selection and the Parquet content checksum are **unchanged**. Nothing moved in source discovery, timing classification, baseline values, calibration values, target availability, prediction ordering or benchmark selection | 2026-08-28 |
| AD-R78 | **The erratum link now comes from the D2 generator, not an append (Task D3-R1)** | Rerunning D2 **erased** the Follow-up section D3 had appended to `docs/d2_target_timing_decision.md`, taking the erratum link with it — the identical failure mode that made D3 move the D1 correction out of a generated file in the first place. The link is now **emitted by `_write_timing_markdown` inside the D2 generator**, so regeneration reproduces it. Caught by rerunning rather than by reading the code, and now asserted by a test that checks both the generator source and the rendered document | 2026-08-28 |
| AD-R79 | **CORRECTION: the D1 restoration was never proven byte-identical to pre-D2 (Task D3-R1)** | `.d2_before.json` had been deleted during D2 cleanup, so the original pre-D2 digests were **unavailable** and exact historical byte identity **cannot be proven**. D3 nonetheless wrote "restored byte-for-byte" in the README, the methodology, the erratum and AD-R74. Recorded correctly now: `original_pre_d2_checksum_available: false`, `restoration_method: inverse_of_known_inserted_block`, `inserted_block_absent_after_restoration: true`, `original_text_retained_after_inverse_patch: true`, `exact_pre_d2_byte_identity_proven: false`, `post_restoration_checksum_pinned: true`, results `54a66c41…`, protocol `80761aab…`. **Those pinned digests are a forward integrity baseline only and are not evidence of the unknown historical checksum** — a test fails if any document presents them as such. The overstated wording is removed from the README, methodology and erratum; AD-R74 is left unedited and is corrected by this entry | 2026-08-28 |
| AD-R80 | The artifact inventory is now generated from the worktree, not counted in prose (Task D3-R1) | D3 wrote "modified 6 files" while listing **seven** paths — it counted the two restored D1 documents as one category. `docs/d3r1_artifact_inventory.json` is now produced programmatically with `created`, `modified`, `restored_by_inverse_patch`, `regenerated_by_rerun`, `deleted`, `generated_gitignored`, `unchanged_verified`, `expected_shared_files` and checksums, and a test asserts every count equals its own list length and every listed path exists. True D3 figures: **14 created, 4 modified, 2 restored**. The blanket phrase "B1–D2 artifacts untouched" is replaced by **B1–D2 production artifacts unchanged, with explicitly documented D1/D2 documentation exceptions** — the two restored D1 documents and the four regenerated D2 documents | 2026-08-28 |
| AD-R81 | **Persistence vs no-contraction is inconclusive on MAE; the registered tie-break decides (Task D3-R1)** | The paired intervals D3 already produced answer the question, so **no further test was run on the same development sample** — no new hypothesis test, no second bootstrap seed, no different block length, no one-sided interval, no post-hoc subgroup, no new tie threshold. h=1 paired difference **+2.3509**, CI **[-2.6478, 6.5716]**; h=3 **+0.3453**, CI **[-2.6858, 4.6333]** — both cross zero. Recorded as `mae_difference_conclusive: false`, `mae_superiority_over_no_contraction: inconclusive`, `tie_break_applied: high_stress_false_negative_count`. The preregistered tie-break resolves it decisively: false negatives **8 vs 21** (h=1) and **4 vs 28** (h=3). Selected benchmarks are **unchanged** — `operational_persistence_latest_published` (h=1) and `operational_persistence_trailing_3m_max` (h=3) — with `benchmark_selection_robust_to_tie_interpretation: true`. **An inconclusive MAE difference does not make the benchmark weak**: persistence wins on point-estimate MAE at both horizons and misses far fewer high-stress events, which is the costly error for an early-warning system, while no-contraction never predicts one at all. No-contraction is **retained as a secondary comparator** (`no_contraction_retained_as_secondary_reference: true`); future models must beat persistence as primary and report no-contraction alongside it. The locked final test remains **unopened** | 2026-08-28 |
| AD-R82 | **D4 is exploratory, not confirmatory, and says so in its own contract (Task D4)** | The frozen specification (alpha **100.0**, channel **`none`**, Brent + Rubber only) is D1's registered conservative fallback, but **D1's development results had already been observed** when D4 froze it. Choosing a specification after seeing outcomes is not preregistration, whatever its provenance. Recorded as `specification_frozen_after_d1_results: true`, `confirmatory_evaluation: false`, `exploratory_development_evaluation: true`, `nested_model_selection_performed: false`, `locked_test_eligible: false`. The fallback was reused deliberately to avoid inventing a tuning process that 15 origins cannot support - D2 (AD-R65) established the history cannot be extended, so a genuine nested selection has nowhere to come from. `fit_fixed_operational_ridge` **raises** on any alpha other than 100.0 or any channel other than `none`; the freeze is enforced, not documented | 2026-08-28 |
| AD-R83 | **NEGATIVE RESULT: commodity features do not beat operational persistence either (Task D4)** | Primary variant `primary_direct_lag2`, macro industry MAE: h=1 **17.5616 vs 17.0945**, h=3 **17.9051 vs 16.6338** - worse at both horizons. Paired difference (model - persistence) h=1 **+0.4672** CI **[-0.2060, 1.3736]**, h=3 **+1.2713** CI **[0.4317, 2.1098]**, the latter **excluding zero against the model**. Both horizons **`incremental_signal_not_supported`**. Against the secondary comparator `operational_no_contraction` (19.4453 / 16.9791) the model is better at h=1 and worse at h=3, both inconclusive. **Event safety PASSED** at both horizons - false negatives **6 vs 8** (h=1) and **1 vs 4** (h=3), the model catching more high-stress months at worse precision, the same trade-off D1 showed. Advancement requires **both** gates, so `development_candidate_supported: false`. This reproduces D1's conclusion under an honest operational contract rather than overturning it | 2026-08-28 |
| AD-R84 | **Half the panel carries no commodity signal at all under direct exposure (Task D4)** | C3's **direct** exposure is exactly **zero for 16 of the 24** Brent/Rubber industry pairs. A zero exposure makes the conditioned predictor a structural constant, and the registered X-only zero-variance rule removes it. For **6 of 12 industries** (IND-01, 03, 06, 07, 10, 11) **every** eligible predictor is such a constant, so no model can be fitted, the residual is `0.0` and the prediction equals the D3 benchmark exactly - **180 of 360 primary rows**, recorded as `model_quality: degenerate_all_predictors_constant_benchmark_only` following D1's registered degenerate-design precedent. Only **IND-04** receives the full 10 predictors; five industries receive 5. The measured difference therefore comes from **6 industries, not 12**, and that is the single most important caveat on AD-R83 | 2026-08-28 |
| AD-R85 | Two training-count deviations from the brief, reported rather than smoothed (Task D4) | h=1 matches exactly: label-permitted **276 / 444**, feature-usable **252 / 420**. h=3 label-permitted matches (**252 / 420**) but feature-usable is **216 / 384**, not the brief's 228 / 396. Cause: the operational h=3 benchmark reads `max(S_{u-3}, S_{u-2}, S_{u-1})`, and at *u* = 2022-03 that window reaches **2021-12, before B3's stress series begins** in 2022-01. No achievable benchmark exists there, so no residual can be defined against one. This is a **series-start boundary, not a release-timing violation**, and the two are kept as distinct exclusion reasons (`insufficient_operational_benchmark_history` vs `insufficient_registered_feature_history`). The row is excluded and reported, never imputed or back-filled, and the deviation with its reason is written into the results JSON | 2026-08-28 |
| AD-R86 | Aluminum and Copper excluded outright; sensitivities cannot promote themselves (Task D4) | Both metals read the **same I/O sector 107** coefficient, so D4 excludes both rather than choosing between them - removing the opportunity to count one shared exposure twice, to select a channel on outcomes, or to report the two as independent measurements. Asserted **separately** against the training matrix, the evaluation row, the scaler and the fitted artifact, so no single check is load-bearing; excluded and ineligible predictors are **omitted, never zero-filled**. IND-06 receives Brent only, with its five Rubber transformations recorded in `omitted_ineligible_predictors`. All **15** origins passed the **day-level** commodity publication check against C1.5's archived issue dates (tightest margin 3 days at 2025-03), so the timing sensitivity is fully populated. **1,080** predictions, 180 per variant-horizon. Sensitivities (timing 17.0253 / 17.3063, structural 18.0460 / 18.1751) are reported separately and **cannot** change the primary status. `approved_for_locked_test: false`, `production_model_approved: false`, locked final test **unopened** | 2026-08-28 |
| AD-R87 | **Direct-zero exposure is a price-STAGE fact, not a missing dependence (Task C5)** | D4's six degenerate industries are explained, not excused. Using the official 2015 NESDC 179-sector table with `T = A + TA`, **Brent direct exposure is exactly zero for 10 of 12 industries** - only IND-04 (petroleum) and IND-05 (chemicals) buy crude across the factory gate - while **indirect exposure runs 7-17%**. Decomposing by the first intermediate sector purchased, the mediators are **Electricity (135)**, **Petroleum refineries (093)** and **Petrochemical products (086)**: IND-06 reaches crude **54.9%** through petrochemicals, IND-07 **21.5%** through refineries, and electricity mediates in nearly every industry. **18 of 48** commodity-industry pairs are direct-zero and **11** retain material indirect exposure at the preregistered 0.01 threshold. Industries buy refined fuel and power, not crude. The decomposition is an exact attribution of the Leontief identity and is explicitly **not a causal claim** | 2026-08-28 |
| AD-R88 | Decomposition reconciles exactly; orientation is pinned against the source flows (Task C5) | Identity residual **6.66e-16**; worst reconciliation against C3's published direct and total values **2.78e-17** - both far inside the required 1e-12. Recorded because it is easy to get wrong: **both `T = A + TA` and `T = A + AT` hold algebraically** for `T = (I-A)^-1 - I`, so the identity alone does **not** pin the matrix orientation. A transposed A satisfies every self-consistency check while dividing each flow by the wrong sector's output. Orientation is therefore verified against the source table (`A = Z/X`), and a test proves a symmetric fixture cannot stand in for that check. Final demand, imports and value added are excluded as intermediate sectors; the zero-output sector 179 is excluded exactly as C3 does; gross-output weights sum to one because the weighted mean of A equals C3's exact `sum(Z)/sum(X)` aggregate | 2026-08-28 |
| AD-R89 | **Brent is stage-misaligned wherever direct exposure is zero (Task C5)** | Price-stage alignment is decided on **semantics, never on accuracy**: a proxy that happened to fit well would still be misaligned. Brent is `stage_misaligned` for all **10** direct-zero industries and `stage_aligned` only for IND-04 and IND-05. Aluminum and copper remain `broad_proxy_with_caution` with the shared-sector-107 warning intact - the coefficient cannot separate them, they are **not** independent measurements, and their exposures are **never summed** (asserted by test). Jewellery-dominated IND-12 exposure keeps its C3 warning. Nothing upstream moved: `direct_exposure` remains the registered primary exposure, total requirement remains a structural sensitivity, and **total requirement was NOT promoted because D4 went badly**, nor was publication_lag_1m promoted for a slightly lower h=1 point estimate, nor was any hybrid exposure chosen by development MAE | 2026-08-28 |
| AD-R90 | D4's no-predictor rows were already correctly labelled; no correction was needed (Task C5) | C5 checked whether D4 describes its degenerate rows as a fitted ridge with an intercept. It does not. D4 already records `model_quality: degenerate_all_predictors_constant_benchmark_only`, `predicted_residual` exactly **0.0** and `predictor_count` **0**, and its schema states the prediction equals the benchmark. C5 therefore records the equivalent vocabulary (`model_fitted: false`, `prediction_source: benchmark_passthrough_no_eligible_predictor`, `residual_correction: 0.0`) as a **metadata clarification only**: `correction_needed: false`, `d4_recomputed: false`. No D4 prediction, metric or status was recomputed or altered | 2026-08-28 |
| AD-R91 | **EPPO ex-refinery petroleum recommended for C6; blocked sources recorded as blocked (Task C5)** | Four official families audited. Only **EPPO_PETROLEUM** passes all ten preregistered criteria: `PRICE STRUCTURE OF PETROLEUM PRODUCTS - EX-REFIN.`, unit **BAHT/LITRE**, an **ex-refinery producer price** reported separately from EXCISE TAX, MUNICIPAL TAX, OIL FUND, CONSERVATION FUND and WHOLESALE - so a retail price cannot be mistaken for a producer price - covering H-DIESEL and FO 600/1500 2%S and mapping to sector **093**, the leading mediator for the direct-zero industries. **Coverage limitation is explicit and unresolved**: only recent daily files are exposed, and the observed `/uploads/YYYY/MM/` pattern returns 404 for 2022-2025 - which is **not** evidence of absence, because 2018 retail files are served from a **2026** folder, so migration re-uploads make folder-date inference unreliable. HEAD is blocked site-wide, so a 403 there is a method restriction. MOC PPI (**403** on every entry point) and ERC (**timeout/404**) are recorded as **blocked, never absent**; they are ineligible only because an unverifiable definition cannot satisfy the gate. BOT FX is audited as an import-price transmission factor and is explicitly **not** an I/O commodity sector price. Ranking used structural and provenance evidence only - the gate **raises** if a candidate carries any performance field: `selection_used_target_outcomes: false`, `d4_metrics_used_for_source_ranking: false`. The recommendation is ingestion approval only (`feature_semantics_approved: false`, `model_feature_approved: false`) and the locked final test remains **unopened** | 2026-08-28 |
| AD-R92 | **The EPPO archive is deep and complete: 5,233 documents, 2002-2026 (Task C6)** | Discovery used the **official WordPress REST media index**, walked to a bounded terminal condition (`api_termination_400_at_page_54`, a documented API termination - not a page count read off the interface). **5,233** price-structure workbooks, **4,976** distinct effective dates, spanning **2002-02-03 to 2026-08-28** at roughly 240 per year. Coverage is **65 of 65 months** for the 2021-01..2026-05 transformation-parity window and **53 of 53** for the 2022-01..2026-05 operational window, with **no missing months in either**. The weekday profile since 2021 (Mon-Fri 259-281 each, Sat/Sun 12 each) indicates working-day publication, but no official schedule document was located, so weekday gaps are recorded `schedule_unresolved` rather than counted as missing issues, and `expected_issue_count` is reported null. Guessed paths and search hits counted as discovery: **zero** | 2026-08-28 |
| AD-R93 | **Values are retrievable but release timing is NOT: point-in-time not supported (Task C6)** | **Every** WordPress media upload timestamp falls in 2026, the site-migration window. A migration timestamp records re-hosting, not first publication - the identical trap D2 found on the OIE i-Index, where sixteen 2016 issues shared one 2017-06-13 stamp - so it is recorded as `migration_timestamp_only` and **never** populates `available_as_of`. Independent third-party captures corroborate **43** archive dates (2020-06/07, 2022-06..08, 2026-08), every one at or after its effective date, but **none coincide** with the 254 validated documents in the audited months, so the supported count is **0 of 254**. Dataset status **`not_supported`**; `full` is reserved for the case where every required observation is evidenced. Recorded separately and never merged: `historical_values_retrievable: true`, `historical_release_timing_verified: false`, `point_in_time_values_supported: not_supported`. Outcome: **`values_retrievable_timing_unresolved`** | 2026-08-28 |
| AD-R94 | Documents are validated against their own content, not their filenames (Task C6) | 256 documents retrieved across 12 complete months; **254 valid, 2 rejected**. Both rejections are `wrong_date_attachment` - the date inside the workbook (2022-12-06, 2025-01-24) disagreed with the archive item date by one day, which filename trust would have missed entirely. Validation checks magic bytes (not the extension), rejects HTML served as a document, requires an internal `PRICE STRUCTURE` title, a parseable internal date agreeing with the item, and at least one real observation. All **five** duplicate-content groups are byte-identical files under the **same** effective date (`duplicate_document`); **none** span incompatible dates. Attachment candidates are ranked by verified relationship to the item, never taken in encounter order - the C1.5-R2 defect | 2026-08-28 |
| AD-R95 | **Two layout regimes and a real diesel definition break, recorded not spliced (Task C6)** | Layout regimes: **`.xls`** (2021-01..2023-01, sheet `โครงสร้างราคา`, label column 0, Excel serial date, tax column **`TAX`**) and **`.xlsx`** (2023-07 onward, sheet `Oil Price Structure`, label column 1, datetime cell, tax column **`EXCISE TAX`**). Product definitions also move: **2024-01 publishes only `H-DIESEL B7`, `H-DIESEL B10` and `H-DIESEL B20` with no plain `H-DIESEL` row**, so the HSD series has a genuine 22-day gap that month. Filling it with a blend would be exactly the substitution the contract forbids, so the gap stands. `FO 600 (1) 2%S` and `FO 1500 (2) 2%S` carry identical labels across both regimes with full coverage. Six semantic regimes recorded (3 series x 2 layouts); the exact displayed label is preserved verbatim including spacing, and normalization decides membership only. All audited series are **BAHT/LITRE**; LPG (BAHT/KILOGRAM) is excluded, and ex-refinery is kept distinct from wholesale, VAT and retail | 2026-08-28 |
| AD-R96 | A price change is not a revision (Task C6) | EPPO publishes a new structure whenever the price changes, so a different value on a different effective date is **ordinary market or policy movement** and never enters revision classification. Revisions are keyed strictly on `(series_id, effective_date, semantic_regime_id)`. Result across the audited subset: **707 `single_vintage_only`, 15 `duplicate_document`, 0 `same_date_revised_value`** - no revisions found, and no daily movement miscounted as one. Byte-identical same-key documents are collapsed losslessly so the canonical key stays unique; a DIFFERING value on the same key would be a revision and is reported, never collapsed | 2026-08-28 |
| AD-R97 | **Eligibility is reported per transformation and everything stays gated on timing (Task C6)** | History depth supports every candidate transformation - the archive reaches 2002, far behind the 2021-01 prehistory a 12-month change referencing 2022-01 requires - so `level_price_history_eligible`, `one_month_change_history_eligible`, `three_month_change_history_eligible`, `three_month_volatility_history_eligible` and `twelve_month_change_history_eligible` are all **true on depth**. That is explicitly NOT a green light: each remains gated on release timing, which is unverified, and the audit records that coverage from 2022-01 alone would have been **insufficient** for twelve-month parity. `monthly_aggregation_contract_approved: false` - no aggregation rule is approved on top of unverified timing, and the three candidate rules are preregistered but unselected. The operational boundary is fixed at the **completed month t-1**, with same-month partial aggregation and post-issue-date observations both forbidden. A **partial audit table of 722 unique observations** exists, labelled partial, carrying no transformation column and joined to no target. `feature_semantics_approved: false`, `model_feature_approved: false`, locked test **unopened**. `xlrd>=2.0` was added as a dependency to read the legacy .xls half of the archive | 2026-08-28 |
| AD-R98 | **DECISION: EPPO approved for release-lag-aware latest-vintage EXPLORATORY use (Task C6.5)** | All **21** C6 invariants were reproduced from production artifacts before deciding: 5,233 discovered documents, 4,976 distinct effective dates, 2002-02-03..2026-08-28, 256 retrieved, 254 validated, 65/65 and 53/53 month presence, 1,315 and 1,076 document-days, 722 canonical rows, 43 captures with 0 overlap, 0/254 verified availability, 707/15/0 revision classes, 22 H-DIESEL gap days in 2024-01, FO labels stable. Decision: **`proceed_exploratory_release_lag_aware_latest_vintage`** - the same footing D3 already put the MPI target on. `latest_vintage_values_used: true`, `historical_release_timing_verified: false`, `point_in_time_values_supported: false`, `fully_real_time_backtest: false`, `eligible_for_exploratory_development: true`, `eligible_for_confirmatory_claims: false`, `eligible_for_locked_test: false` | 2026-08-28 |
| AD-R99 | **The 2-month lag is a POLICY assumption, not a measured delay (Task C6.5)** | **No publication delay was measured.** `minimum_verified_publication_lag_months` is **null** and stays null; deriving it from sparse capture upper bounds is forbidden. The project imposes `operational_policy_lag_months: 2` with `operational_lag_is_measured: false` and `operational_lag_basis: conservative_project_policy`. At issue month t an EPPO observation may refer at latest to **t-2**; `same_month_use_permitted: false` and `publication_lag_1m_use_permitted: false`. Availability fields are kept apart so an assumption cannot masquerade as provenance: **`source_available_as_of` stays null** for every historical observation and `observation_availability()` **raises** if a policy-derived month is passed into it; `policy_available_month` carries `reference_month + 2` and `availability_basis` names which is which | 2026-08-28 |
| AD-R100 | **CORRECTION to C6 wording: inspected-source limits are not universal impossibility (Task C6.5)** | C6 stated the legacy archive was **gone**. That exceeded the evidence: the tested legacy URLs no longer expose it, which is not the same claim. Superseded wording is recorded and replaced: `legacy_archive_status: tested_legacy_urls_no_longer_expose_original_archive`; `historical_timing_status: not_reconstructable_from_sources_inspected_in_c6` (**not** "can never be verified"); `monthly_presence_coverage: complete` **with** `complete_daily_document_validation: false`, because 65/65 month presence means at least one document per month and **not** a validated daily archive; and archive depth alone does not make any transformation feature-ready. Recorded explicitly: **absence from the inspected mechanisms is not proof that no historical publication evidence exists anywhere** - offline records and a direct request to EPPO were never exercised. Previous entries are preserved; this supersedes by addition | 2026-08-28 |
| AD-R101 | **The 43 captures support nothing: all are identity-unverified (Task C6.5)** | Re-audited under an explicit taxonomy, **all 43** classify as `capture_identity_unverified`: the CDX index was read but the archived bytes were **never fetched** and the document identity **never validated**, so **0 usable upper bounds** remain and no delay statistic is reported. Even a fully validated capture would establish only that the document was public **by** the capture date - an **upper bound**, never a first-release date (`proves_first_release_date: false` on every record, unconditionally). Observed capture lags span **0 to 397 days** (median 34), which describes archiver visiting behaviour, not publisher behaviour. The 31-day contemporaneity window is a preregistered **classification convention**, not an empirical finding | 2026-08-28 |
| AD-R102 | Presence, ingestion and readiness are five states and two stages (Task C6.5) | Five ingestion levels are tracked separately: `monthly_document_presence_verified: true`, `complete_daily_inventory_discovered: true`, `complete_daily_documents_downloaded: false`, `complete_daily_documents_validated: false`, `monthly_aggregation_contract_approved: false`. The **254** validated documents are an audit sample spanning all required months, **not** a daily ingestion of the **1,315** discovered document-days, and the 722-row table stays a partial audit table. Transformation status replaces one ambiguous `eligible` with two stages: every candidate (level, 1m, 3m, 12m, 3m volatility) has `historical_depth_sufficient: true` but **`source_ready_for_transformation: false`**, blocked on daily ingestion, the aggregation contract and availability enforcement. No transformation was created | 2026-08-28 |
| AD-R103 | **Series decisions and the C7 authorization boundary (Task C6.5)** | `FO 600 (1) 2%S` and `FO 1500 (2) 2%S` (both BAHT/LITRE) are `eligible_for_c7_full_archive_ingestion: true`, `semantic_status: stable_in_audited_window`. **H-DIESEL is `conditional`**: 22 days in January 2024 publish only B7/B10/B20, so the gap **stands** - `silent_blend_substitution_permitted: false`, `imputation_permitted: false`, and January 2024 is neither filled nor silently dropped. C7 is authorized for **full archive ingestion and monthly source-table construction only**: `c7_full_archive_ingestion_authorized: true`, `c7_monthly_source_aggregation_authorized: conditional` (until the complete required daily archive is retrieved and validated), `c7_predictive_feature_creation_authorized: false`, `c7_target_join_authorized: false`, `c7_modeling_authorized: false`. `feature_semantics_approved: false`, `model_feature_approved: false`, D4 metrics **not** recalculated, locked test **unopened** | 2026-08-28 |
| AD-R104 | **The full archive was retrieved and the denominator kept honest (Task C7)** | All **21** C6/C6.5 invariants were reproduced from production artifacts before anything was computed, and the run raises `SystemExit` if one fails. Every officially discovered document-day in 2021-01-01..2026-05-31 was attempted: **1,336** media records across **1,315** distinct document-days (**19** days carry more than one attachment), **1,336** retrieval attempts, **1,333** HTTP successes, **1,331** content-valid documents and **1,310** valid canonical document-days. **5** discovered document-days remain unresolved and are reported as unresolved inventory items, never as absent. `assert_denominator_consistent` **raises** if a later stage claims more than an earlier one could support, so 1,315 discovered days can never be reported as 1,315 validated days. Acquisition is resumable and idempotent: progress is checkpointed to `data/raw/_manifests/EPPO_PRICE_STRUCTURE_manifest.jsonl` keyed by WordPress media ID, a rerun re-verifies bytes on disk by checksum instead of re-fetching, a valid file is never deleted because a later request failed, and magic bytes are checked **before** anything is written so an HTML challenge page is never stored under an `.xls` or `.xlsx` name. Polite rate 0.35 s, 3 bounded attempts, backoff (2 s, 6 s), throttle abort after 8 consecutive refusals. Raw workbooks (~54 MB) and both generated Parquet tables stay git-ignored | 2026-08-29 |
| AD-R105 | **Three official media records point at files that do not exist (Task C7)** | 2026-04-17, 2026-04-18 and 2026-04-19 have media records that the EPPO REST API still serves with HTTP 200 and a declared `filesize`, but the attachment URLs those records publish return **404** on every attempt, while the neighbouring 2026-04-16 file downloads normally. That is an **actual unresolved inventory item**, categorically different from a weekday with no discovered entry: `expected_issue_count` stays **null** and `expected_schedule_status: unresolved` because EPPO documents no publication schedule, so an unlisted weekday is never counted as a missing document. The three days block approval for 2026-04 with `unresolved_document_retrieval`. Separately, **two wrong-date attachments** persist from C6: `pt-price-st-2022-12-5.xls` contains a document dated 2022-12-06 and `pt-price-st-2025-1-25.xlsx` one dated 2025-01-24. Each is rejected for its listing date and **not reassigned** to its internal date, and **no separately valid official document covers either date** - so 2022-12 and 2025-01 carry `unresolved_document_identity`. Retrieval failure and identity failure are kept as distinct statuses because they lead to different follow-up work | 2026-08-29 |
| AD-R106 | **Layouts were discovered from the documents; a third regime exists (Task C7)** | Every sheet of every document is inspected and every price stage resolved by **column label**. There is no positional fallback: an unresolvable header is rejected as `unknown_layout`, `assert_regime` **raises** when a document is forced into a layout it does not have, and `discover_layout` **raises** if the EX-REFIN. column resolves to the same index as wholesale, excise tax, VAT or retail. The previously `unsampled_transition_interval` **2023-02-01..2023-06-30** was inspected in full (99 documents, all valid) and resolves into the two known regimes with **no additional regime inside it**; the container change falls between **2023-06-02** (last `.xls`) and **2023-06-06** (first `.xlsx`). Spans: `xls_2021` **2021-01-04..2023-06-02** (584 documents), `xlsx_2023` **2023-06-06..2026-05-31** (746). A **third** signature exists and is registered only because an actual document supports it: **2024-03-07** is an `.xlsx` carrying the Thai sheet name `โครงสร้างราคา` with the `EXCISE TAX` column. Tax-column renaming was **not** the only layout change - the workbook also grows from one sheet to three from 2024-07-23 | 2026-08-29 |
| AD-R107 | **CORRECTION by extension: ordinary H-DIESEL is absent for 305 days, not 22 (Task C7)** | The full archive shows ordinary `H-DIESEL` absent for **305 consecutive document-days, 2023-09-18 to 2024-12-17** - fifteen months, not the one month C6 reported. C6 is **extended, not contradicted**: C6 sampled 254 of 1,315 document-days and every sampled day that fell inside the absence was in January 2024, so its statement was true of what it observed and understated the extent. Blend definitions are published throughout (`H-DIESEL B7` 305 days, `H-DIESEL B20` 276, `H-DIESEL B10` 151) and **none is substituted**: `H-DIESEL B7` last appears 2024-12-17, exactly where plain `H-DIESEL` returns. Fourteen H-DIESEL months carry `monthly_value: null` with `aggregation_status: semantic_definition_gap` and `quality_flag: ordinary_h_diesel_not_published_blend_substitution_prohibited`; the two straddling months (2023-09, 2024-12) are **also null** rather than a mean over the surviving days, because a partial month is not comparable with a full one under the same label. January 2024 is neither filled, nor interpolated, nor zeroed, nor silently removed. `H-DIESEL 20` appears on four days in 2026 in place of `H-DIESEL B20` and is recorded as a blend label, never as the audited product | 2026-08-29 |
| AD-R108 | **A fuel-oil label variant is surfaced, not resolved (Task C7)** | For **66 document-days, 2024-07-23 to 2024-10-28**, the archive drops the table's ordinal index from both fuel oils: `FO 600 2%S` and `FO 1500 2%S`, never on the same day as the ordinal labels. Discarding 66 valid official documents would misdescribe the archive, so they are parsed and kept with `label_variant_status: unconfirmed_label_variant`; `assert_exact_product_label` **raises** if a variant is used as the audited series. `equivalence_established: false` - whether `FO 600 2%S` and `FO 600 (1) 2%S` name the same series is a semantic decision, and an ingestion task may surface it but not settle it - so all **8** affected product-months (2024-07..2024-10 for both grades) carry `unit_or_definition_break` with a **null** value. 2024-07 and 2024-10 additionally mix both labels within one month. This is the substitution rule applied to a case C6 could not see: C6 sampled no day inside the interval | 2026-08-29 |
| AD-R109 | **Monthly source table built; the approval gate FAILED and was not forced (Task C7)** | Rule `arithmetic_mean_of_validated_observed_official_document_days` version `c7_v1`, preregistered before retrieval and selected from source semantics, archive completeness and deterministic reproducibility - `selected_from_predictive_performance: false`, and no predictive quantity exists in this task. Each distinct valid official document-day counts **once**; nothing is synthesised for a weekend, no unchanged price is forward-filled, no value is duration-weighted, no partial issue month is used, and `assert_statistic_description` **raises** on `calendar_day_mean`, `trading_day_mean`, `transaction_price` or `point_in_time_monthly_mean`. **3,625** canonical daily rows and **195** monthly rows (3 series x 65 months, every month present including null ones). **162** product-months `approved`; **16** `semantic_definition_gap`, **8** `unit_or_definition_break`, **6** `unresolved_document_identity`, **3** `unresolved_document_retrieval`. Because 2022-12, 2025-01, 2026-04 and 2024-07..2024-10 do not pass for the fuel oils, **`monthly_aggregation_contract_approved: false`** - reported as a gate failure, and no H-DIESEL value was fabricated to unblock anything. Availability is executed, not restated: `source_available_as_of` is **null** in all 195 monthly and all 3,625 daily rows, `policy_available_month` is `reference_month + 2` everywhere, and the daily column is named `candidate_monthly_policy_available_month` so a monthly policy is never read as a measured daily date. `select_monthly_source_available_at_issue` permits 2023-11 at issue 2024-01, 2025-01 at 2025-03, 2026-03 at 2026-05, and **raises** on a mis-stamped row or a row carrying `source_available_as_of`. `source_ready_for_transformation: false` for all three series; no transformation, no exposure multiplication, no MPI join, no model, locked test **unopened** | 2026-08-29 |
| AD-R110 | **DECISION: the fuel-oil ordinal is not part of the product identity (Task C7.5)** | All **24** C7 invariants were reproduced before deciding. The question was whether `FO 600 2%S` (66 document-days, 2024-07-23..2024-10-28) names the same series as `FO 600 (1) 2%S` (679 days). It does, and the evidence is the publisher's own: from 2024-07-23 the workbook carries a **Thai companion sheet whose product rows are formula-bound to the English sheet** - the Thai row 14 price cell reads `='Oil Price Structure'!C14`. Across all **471** three-sheet documents that Thai name is byte-identical, `น้ำมันเตา 600 กำมะถันไม่เกิน 2%`, and it binds to the ordinal form in **405** documents and to the ordinal-free form in the other **66**. One product name, no ordinal, both English forms - a source-controlled data dictionary inside the official document. Structural corroboration is uniform across all **745** xlsx documents (unit `UNIT: BAHT/LITRE`, stage `EX-REFIN.`, product number, `2%S`, row neighbours, identical excise/municipal/fund treatment, **zero** documents carrying both forms, the forms replacing one another exactly at 2024-07-23 and 2024-10-29). **Numeric continuity was recorded and is NOT load-bearing** (`numeric_evidence_load_bearing: false`) - a different product at the same table position would look equally continuous, and `assert_not_numeric_only_equivalence` **raises** on a numbers-only case. Decision **`equivalence_established`**, rule `fo_ordinal_index_dropped_v1`; the eight-condition gate passed with zero failures for both grades | 2026-08-29 |
| AD-R111 | What `(1)` and `(2)` MEAN is still unknown, and the decision does not need it (Task C7.5) | `ordinal_marker_meaning: not_established_from_inspected_sources`. **No workbook in the 1,331-document archive** carries a note, cell comment, defined name, hidden row, hidden column, hidden sheet, header, footer or glossary entry explaining the ordinals - the `คำอธิบาย` glossary present in 471 documents explains the **eleven columns**, not the products - and the 585 legacy `.xls` documents contain only the generic public-reference disclaimer. No EPPO policy or formula document naming them was located through the REST media index, posts, pages, the official `oil-api/v1` route index or the `oil-structure-prices` endpoint (which does confirm `FO 600 (1) 2%S` as the current product name). That is a **limit of the sources inspected, not proof that no explanation exists anywhere**. Recorded rather than guessed: the plausible reading that the ordinals are Thai fuel-oil type numbers was NOT asserted, because no inspected source states it. Equivalence rests on the bilingual binding, which does not require knowing what the marker means. The **raw displayed label is never overwritten** - `FO 600  2%S` (with the double space the deleted ordinal left behind) stays in the row and `canonical_product_id` sits beside it; `assert_raw_label_preserved` **raises** on a rewrite | 2026-08-29 |
| AD-R112 | **Two wrong-date documents recovered on official evidence; three files stay unretrievable (Task C7.5)** | C7 refused to reassign two attachments whose internal dates disagreed with their filenames. The **parent posts** settle it: `pt-price-st-2022-12-5.xls` hangs off a post titled `โครงสร้างราคาขายปลีกน้ำมัน 6 ธันวาคม 2565` published **2022-12-06**, and the document's own internal date is **2022-12-06**; `pt-price-st-2025-1-25.xlsx` hangs off a post titled and published **2025-01-24**, matching its internal date. **Two official sources independent of the filename plus the document's own content** is a documented recovery, not the silent reassignment C7 refused - `classify_recovery` **requires two** corroborating sources and returns `wrong_date_attachment_unresolved` on one or none. Neither internal date had its own discovered inventory entry, so nothing is double-counted; **2022-12 and 2025-01 are now complete** (19/19 and 22/22 document-days). The three April 2026 files are **`official_attachment_unretrievable`**: their media records still return HTTP 200 with a declared `filesize`, but the files 404 on the production host **and** on the `eppo.d-gis.com` host named in their own `guid`, the parent post lists them, no alternate media record exists and the attachment pages are 404. Recorded with the required wording `official_document_not_recovered_from_inspected_channels`, never as a claim that the documents never existed. Recovery was bounded to seven official channels per target, no filename was guessed, no web archive was used, and no message was sent | 2026-08-29 |
| AD-R113 | **H-DIESEL is excluded from C8, and the exclusion is about continuity not validity (Task C7.5)** | Ordinary `H-DIESEL` is absent for **305 consecutive document-days, 2023-09-18 to 2024-12-17**, spanning **sixteen** reference months. `eligible_for_c8_primary_transformation: false`, `eligible_for_c8_sensitivity_transformation: false`, `status: not_ready_long_semantic_definition_gap`, `c8_hsd_transformation_authorized: false`. Recorded explicitly: **this does not mean the EPPO documents or the blend products are invalid** (`documents_invalid: false`, `blend_products_invalid: false`) - both are valid official publications. It means ordinary H-DIESEL cannot support a continuous, consistently defined source series for the intended task. `blend_substitution_permitted: false` and `imputation_permitted: false` stand; every H-DIESEL observation and all blend-label provenance are **preserved**, and the 48 complete H-DIESEL months keep their values. The two months straddling the gap boundary (2023-09, 2024-12) carry **no value in either view** rather than a mean over the surviving days, because a partial-definition month is not comparable with a full one under the same label | 2026-08-29 |
| AD-R114 | **One approval boolean was answering three questions; it is now three fields (Task C7.5)** | C7's `monthly_aggregation_contract_approved` conflated *is the rule sound*, *is every month covered* and *may the series be transformed with its gaps propagating*, forcing all three to the worst of them and reporting a sound method as a failed one. Replaced by `aggregation_method_semantics_approved` (**true** for all three series - the rule `arithmetic_mean_of_validated_observed_official_document_days` is valid and reproducible), `full_requested_window_coverage_complete` (**false** for all three) and `series_ready_for_transformation_with_explicit_gaps` (**true** for both fuel oils, **false** for H-DIESEL). `assert_method_approval_is_not_coverage` **raises** on wording that reads the first as the second. Every month now carries **two values**: `monthly_value_strict`, null unless the month's entire discovered inventory validated and the only value a transformation may read, and `monthly_value_descriptive`, which may summarise the days that exist and is always labelled incomplete - `assert_transformation_input_permitted` **raises** if a descriptive value is offered as an input. April 2026 is the case that made this necessary: a real 24-day mean exists and is reported descriptively, while the strict value stays null because three discovered days never validated. Final counts across 195 product-months: **168** `approved_complete_inventory`, **8** `approved_with_source_label_variant`, **16** `semantic_definition_gap`, **3** `known_document_gap` | 2026-08-29 |
| AD-R115 | Coverage has two views, and the fuel oils stay apart (Task C7.5) | The **full source audit window** is 2021-01..2026-05. The **operational** view is derived from preregistered issue-month ranges and the two-month policy lag only, with `target_outcomes_read: false`: the maximum preregistered issue origin is **2026-04** (locked test, h=1), so the maximum source reference month any existing key requires is **2026-02**. April 2026 therefore reaches **no currently preregistered issue origin** - and is documented regardless, as required. Reach is reported twice because reporting only the direct one would understate it: a level feature reaches one origin (`m + 2`), a twelve-month change reaches thirteen (`m+2 .. m+14`). Under the direct view the H-DIESEL gap months 2023-11..2024-12 hit **development** origins; under the prehistory view 2024-05 onward also hit **purge** and **locked-test** origins. On the shared channel: FO 600 and FO 1500 both read I/O sector **093** and the coefficient cannot separate them, so `additive_aggregation_allowed`, `simple_average_allowed`, `automatic_composite_index_allowed` and `simultaneous_model_entry_approved` are all **false**, `assert_no_additive_fuel_oil_aggregate` **raises** on any attempt to combine them, and choosing between them by target performance is prohibited. Both series are preserved as **separate, mutually exclusive channel variants**. The semantic overlay (`c7_5_semantic_v1`, 195 rows, git-ignored) is a layer above the immutable C7 tables and **does not overwrite the C7 monthly audit artifact**. C8 is authorized for FO 600 and FO 1500 source-level transformations with gaps propagating; industry conditioning, target joins, imputation, outcome-based channel selection, modelling and locked-test evaluation are all **false** | 2026-08-29 |
| AD-R116 | **C8 reuses C2's formula implementation rather than re-deriving it (Task C8)** | All **24** C7.5 invariants were reproduced before anything was computed. The five transformations - price level, 1m/3m/12m log change and 3m realized volatility - are computed by **importing** `log_change_pct`, `realized_volatility_pct` and the preregistered `FEATURE_DEFINITIONS` from `features.commodity_features`, not by writing a second copy. `assert_c2_formula_compatibility` re-checks the input-lag tuples so a change on either side **raises** instead of drifting: the failure it prevents is invisible, because a `ddof=1` here or a `log10` there would still produce plausible numbers on both sides. Natural logarithm, scaled by **100**, volatility `ddof=0`. Realized volatility declares **FOUR** price levels `{m-3, m-2, m-1, m}`, not three, because `r_{m-2}` is itself a change from `m-3`; declaring three would understate both the lineage and the availability requirement. The 3m and 12m changes are **endpoint** definitions and are never a sum of rounded monthly changes | 2026-08-29 |
| AD-R117 | **Matching formulas are not matching evidence, and C8 records both (Task C8)** | `formula_compatible_with_c2: true` **and** `source_timing_contract_differs_from_c2: true`, with `equivalent_timing_quality_implied: false`. The differences are stated field by field: C2's World Bank inputs are **archived first-release** values whose publication timing was **measured** and which support point-in-time use; C8's EPPO inputs are **latest-vintage** values under a **two-month policy lag that was never measured** and which support no point-in-time claim. Identical arithmetic on differently-evidenced inputs is still differently-evidenced, and the audit refuses to let the shared formula imply otherwise | 2026-08-29 |
| AD-R118 | **The grid is complete on purpose: 650 rows, 598 finite, 52 null (Task C8)** | 2 channels x 65 reference months x 5 transformations. **No cell is dropped for being undefined**: an omitted row and a null row are indistinguishable downstream only when the null row is missing, which is exactly how a gap becomes invisible. Finite counts per series - price level **64**, 1m **62**, 3m **61**, 12m **52**, volatility **60**, total **299** - were **preregistered before the run** in `configs/eppo_fuel_oil_transformations.yaml` **and** derived independently from the declared input lags at run time by `expected_counts()`. The runner **stops** if the preregistered table, the derived counts and the observed counts disagree; all three matched exactly for both channels. `counts_altered_after_observing_output: false` | 2026-08-29 |
| AD-R119 | **Missingness precedence is total, and prehistory is never called a source gap (Task C8)** | Seven statuses in a fixed order - `unauthorized_source_series`, `source_unit_mismatch`, `insufficient_feature_history`, `current_month_source_gap`, `input_window_source_gap`, `nonpositive_source_value`, `available_numeric` - so two runs cannot disagree about which kind of absence a cell has. Of the **52** nulls: **38** `insufficient_feature_history` (a window reaching before 2021-01, which is a property of the window and not a fact about the archive), **10** `current_month_source_gap` (all five transformations at **2026-04**), and **4** `input_window_source_gap` (the 1m change and the volatility at **2026-05**, each missing 2026-04). A value is produced **only** when every declared input month is present and approved: no imputation, interpolation, forward fill, backward fill, clipping, winsorization or normalization, and **no substitution from the other fuel oil**. Only `monthly_value_strict` is carried into a transformation - `monthly_value_descriptive` appears nowhere in the C8 module or runner, so C7.5's descriptive 24-day April mean cannot reach a formula | 2026-08-29 |
| AD-R120 | Availability is derived from the input window and verified, not hard-coded (Task C8) | `policy_available_month` is the **maximum** policy month over every declared input, and the equality with `reference_month + 2` is **checked on all 650 rows** by `assert_policy_month_matches_reference_rule` rather than assumed - a transformation that ever reached forward would fail the check instead of quietly inheriting a stamp that is too early. `source_available_as_of` is **null in every row**: EPPO release timing was never measured and the conservative policy month is not a release date, which `assert_policy_month_is_not_a_release_date` **raises** on. The selector returns only **numeric** rows for **exactly one** channel and refuses a request naming both; latest permitted reference months are **2023-11** at issue 2024-01, **2025-01** at 2025-03 and **2026-02** at 2026-04. Issue-key coverage was audited from preregistered issue-month ranges alone (`target_values_read: false`): all **15** development, **3** purge and **10** locked-test issue months have every transformation available at their latest permitted reference month, for both channels. The **2026-04 gap is reachable by no registered issue key** and is kept and reported anyway - `removed_because_operationally_unused: false` | 2026-08-29 |
| AD-R121 | **Null rows carry lineage too, and the fuel oils stay apart (Task C8)** | A C8 lineage digest hashes the **specification as well as the inputs** - transformation, formula version, channel, reference month, unit, availability-policy version, input ordering, output value or null status, missingness reason, and the expected, available and missing input months - so a null row still explains itself and two nulls with different causes do not collide. C2's `lineage_checksum` refuses an empty input list, which is correct there and unusable here, so C8 has its own. All **650** rows carry a digest and all **650 are distinct**; the **52** null rows all carry complete lineage. `transformation_lineage_record` **raises** if a value is produced while inputs are missing, or if available plus missing does not account for the declared window. On the shared channel: FO 600 and FO 1500 both read I/O sector **093**, so `additive_aggregation_allowed`, `simple_average_allowed`, `automatic_composite_index_allowed` and `simultaneous_model_entry_approved` are all **false** and `assert_single_fuel_oil_channel` **raises** on any sum, mean, index, PCA or simultaneous request. Both variants coexist in one audit table only because the channel ID is part of the key. Both channels are `source_transformations_ready_for_structural_conditioning: true`, which is **not** model approval: `industry_conditioned_features_created`, `feature_semantics_approved` and `model_feature_approved` are all false, no exposure coefficient or MPI target was read, and the locked test remains **unopened** | 2026-08-29 |
| AD-R122 | **EVERY industry buys refinery products: sector-093 direct exposure has no zeros (Task C9)** | C3 measured sector **031** (crude) and found **10 of 12** industries at direct-zero; C5 explained why. C9 measures sector **093**, *Petroleum refineries*, on the same official table with the same column orientation `A[093,j] = Z[093,j]/X_j` and the same gross-output weights, and finds **zero** observed-zero exposures and **zero** unresolved across all twelve industries. Range **0.004748** (IND-12) to **0.064156** (IND-05), median **0.009931**. The heaviest are chemicals (IND-05, **0.0642**, 76% of it through petrochemicals 086), non-metallic minerals (IND-07, **0.0346**, led by cement 102 and glass 100) and basic metals (IND-08, **0.0269**, led by iron and steel 105). This is the mirror image of the crude result and the clearest confirmation of C5's finding. Exposure is derived **twice by different routes** - output-weighted mean and the exact summed-flow ratio - which are algebraically identical, so a disagreement means a weight, a filter or the orientation is wrong; worst residual **1.39e-17** against a 1e-12 requirement. `assert_sector_is_093` **raises** on sector 031 by name, total requirement is never computed (`leontief_inverse` is not imported), and no exposure is normalised across industries | 2026-08-29 |
| AD-R123 | **The structural decision is frozen BEFORE any price is loaded, and that is checkable (Task C9)** | Phase A may read the NESDC matrix, the sector definitions, the C3 crosswalk and C5 findings - nothing else. `assert_no_numeric_source_values` **raises** if `feature_value`, `monthly_value_strict`, `conditioned_value` or any named transformation reaches a Phase-A function at any nesting depth, and the Phase-A modules import no `features` module at all. The 24-row decision table is hashed, written to disk, **re-read and re-verified before** the C8 parquet is opened, and **verified again after** conditioning. Frozen digest `d528aba4aedcb8b8051ad618d66d49c71239267fb26fd2ead292ee90b12c9703`; `assert_phase_a_frozen` **raises** on any eligibility change. Expected Phase-B counts were derived from the frozen eligibility - **6,578** numeric (598 x 11), **572** source gaps (52 x 11), **650** ineligible (650 x 1), **7,800** total - and reconciled exactly against what was built | 2026-08-29 |
| AD-R124 | **Sector 093 is a nine-product basket, so fuel oil is a broad proxy and nothing better (Task C9)** | NESDC's own definition of sector 093 reads: *the products of this sector are gasoline, jet oil, LPG, asphalt, paraffin, sulfur, kerosene, diesel and fuel oil*. A positive coefficient therefore proves purchase **from the refinery-products sector**, not purchase of FO 600 or FO 1500, and `assert_not_inferred_from_coefficient` **raises** on that inference. The 179-sector table does not split refinery products by type and no source Phase A may read identifies which product an industry buys, so **all twelve industries are `broad_proxy_use_with_caution` at `medium` confidence** - never `fit_for_structural_use`, never `high`, and `product_specific_evidence_available: false` everywhere. Recorded as a limit of the permitted sources, **not** as proof that no such evidence exists anywhere: EPPO's own energy-consumption statistics were outside the Phase-A read list and were not consulted | 2026-08-29 |
| AD-R125 | **IND-04 contains sector 093, so its direction is mixed and it is ineligible (Task C9)** | Eleven industries purchase refinery products without producing them: `cost_pressure`, multiplier **+1**, eligible. **IND-04 (Refined Petroleum Products) maps to sectors 093 and 094**, so a higher refinery-product price is simultaneously its revenue and its input cost - `mixed_or_ambiguous` with a **null** multiplier, **ineligible** for the default conditioned feature, and `assert_direction_not_forced` **raises** on any attempt to give it +1. The pair is **retained** in the structural audit table, never deleted. Realized volatility carries its own reading throughout, `disruption_magnitude_nonnegative`, so the sign of a level is never conflated with the magnitude of a wobble. Zero, unresolved and ineligible stay three distinct states: an observed-zero exposure would remain **eligible** with `quality_flag: observed_zero_direct_exposure` (none occurred) | 2026-08-29 |
| AD-R126 | **The price stage and the valuation basis do not match, and the product says so (Task C9)** | EPPO measures the **ex-refinery** stage - before excise tax, municipal tax, the oil and conservation funds, wholesale components, VAT, marketing and retail margins. The C3 coefficient is in **purchasers' prices**, import-inclusive. Their product is recorded as `partial_mismatch_purchasers_price_coefficient_x_ex_refinery_price` with `structural_interaction_only: true`, `monetary_cost_measure: false`, `elasticity: false`, `causal_effect: false`, and `assert_valuation_claim_permitted` **raises** on any description as baht cost per unit of output, an elasticity, a pass-through rate, a causal effect or a forecast coefficient. Units keep the coefficient visible rather than absorbing it: `baht_per_litre_x_io_coefficient`, `percentage_point_log_change_x_io_coefficient`, `percentage_point_log_change_volatility_x_io_coefficient` | 2026-08-29 |
| AD-R127 | **7,800 conditioned rows with three kinds of absence kept apart (Task C9)** | 2 variants x 12 industries x 65 months x 5 transformations, and **no row is dropped** for being ineligible, null or zero. Status precedence is fixed: **structural ineligibility -> source null -> structural zero -> value**, so an ineligible pair is `null` and never `0`, a missing source month is `null` and never `0` **even where exposure is zero**, and an observed-zero exposure is exactly `0.0` **only** when the price exists. `assert_null_not_converted_to_zero` **raises** on both collapses. Observed: **6,578** `available_nonzero`, **650** `not_generated_due_to_direction_ambiguity`, **418** `source_insufficient_history`, **110** `source_current_month_gap`, **44** `source_input_window_gap`. Availability is `max(source policy month, 2020-03)` - the I/O table's **publication** month, never its 2015 reference year and never a download timestamp - and the source policy dominates in all **7,800** rows with `source_available_as_of` null throughout. Lineage hashes **both** provenance chains and all **7,800** digests are distinct, including the **1,222** null rows | 2026-08-29 |
| AD-R128 | Development snapshot built; the two variants stay mutually exclusive (Task C9) | **1,800** cells (15 issue months x 2 variants x 12 industries x 5 transformations) at the latest reference month the two-month lag permits: **1,650 numeric**, **150 `masked_ineligible`** (IND-04), **0** rejected for a source null, because every development reference month 2023-11..2025-01 has complete source data. Purge (**3** keys, 2025-02..2025-04) and locked (**10** keys, 2025-05..2026-02) coverage is reported from **issue-key ranges only**, `materialised: false`, with `target_values_read`, `model_predictions_read` and `locked_test_outcomes_read` all false. Both variants carry the **SAME** sector-093 vector, which is exactly why `assert_no_fuel_oil_combination` **raises** on any sum, mean, composite index, PCA or simultaneous request - including one framed as keeping the channel with the better development MAE. They coexist in one audit table only because the variant ID is part of the key. `industry_conditioned_features_created: true`; `feature_semantics_approved`, `model_feature_approved`, `target_join_authorized`, `modeling_authorized` and `locked_test_evaluation_authorized` all remain **false** - a structurally conditioned feature is not automatically predictive, and the locked test stays **unopened** | 2026-08-29 |
| AD-R129 | **The 165-row panel is fifteen rows repeated eleven times, and C10 proves it (Task C10)** | C9's conditioning is `D[v,g] = E[g] · X[v]`: every eligible industry's design is ONE 15 x 5 source-time matrix scaled by its own positive sector-093 exposure. Three checks confirm it numerically rather than by argument. **Cross-sectional rank is 1 at all 15 issue months** for both variants — the cross-section is the outer product `E x[v,t]ᵀ`, so anything above 1 means a conditioning, exposure, alignment or pivot error. **The source-time matrix, every one of the eleven industry matrices, and the 165-row stacked panel all have rank 5** — the panel gains rows by repeating, not by informing. **Exposure normalisation recovers one matrix from all eleven industries**: worst residual **7.11e-15** (fo600) and **3.55e-15** (fo1500) against a preregistered 1e-9, and **1.78e-15 to 3.55e-15** against the C8 source transformations themselves. Consequently every primary diagnostic runs on the **15 x 5 source-time matrix**; `assert_primary_input_is_source_time` **raises** when the stacked panel is handed to a rank or correlation routine, `assert_no_independence_claim` **raises** on wording that calls those rows independent, and `stacked_rows_are_independent_claim_permitted` and `temporal_independence_claim_permitted` are both **false** — adjacent transformations overlap in time, so consecutive monthly rows may be serially dependent | 2026-08-29 |
| AD-R130 | **Sector-093 exposure does not survive per-industry standardisation, so the conditioning adds scale and no temporal shape (Task C10)** | For `E > 0`, `standardize(E·X) == standardize(X)` column-wise because the positive scalar cancels in both the mean and the standard deviation. Observed max residual **4.39e-15** (fo600) and **2.66e-15** (fo1500) against a preregistered 1e-9, and **11 distinct raw industry designs collapse to one equivalence class** once normalised and once standardised, per variant. Reported as `conditioning_adds_cross_industry_scale: true`, `conditioning_adds_temporal_degrees_of_freedom: false`, `per_industry_standardization_removes_exposure_scale: true`. This is a **property of the design matrix**, established without reading any target: `assert_no_performance_claim` **raises** on "will predict", "predictive power", "adds no signal" and the rest, and `predictive_utility_assessed` stays false. IND-04 is **never** exposure-normalised — its direction is unresolved and its multiplier null, so dividing its values by its exposure would reconstruct a design that does not exist, and `assert_positive_exposure` **raises** on the attempt by name | 2026-08-29 |
| AD-R131 | **Thresholds were hashed before the first value loaded, and one that failed was reported rather than moved (Task C10)** | Rank tolerance **1e-10**, near-zero variance **1e-12**, reconstruction and standardisation tolerances **1e-9**, standardisation **ddof 0**, the expected 180 x 5 dimensions, the missingness precedence and the thirteen permitted statistics were written into `configs/fuel_oil_development_matrix.yaml` and hashed to `1c88cdd7f2017aa94ee1b069be436c469ee08adcf649fe6ad4c981f995eb2041` **before** any C9 feature value was read; `assert_contract_frozen` re-asserts the digest immediately before the load and **again after the diagnostics**, and **raises** on a tolerance changed in between. One preregistered expectation **did not reproduce**: the config expected one distinct standardised fingerprint per variant and fo600 produced **11**. The cause is the fingerprint, not the design — it rounds to 13 *significant* digits, so two `price_level` cells whose standardised values sit near zero are resolved below double precision. The decision rule is the frozen **absolute** tolerance, which places all eleven industries in **one** equivalence class at a 4.39e-15 residual. The discrepancy is published in full with `thresholds_changed_after_observation: false` and `expectations_relaxed_to_obtain_a_pass: false`; the fingerprint count is reported unchanged beside it | 2026-08-29 |
| AD-R132 | **Degeneracy is a finding, and the design was not edited to remove it (Task C10)** | Both 180 x 5 matrices keep **all five transformations and all twelve industries**: **0** exact-zero-variance columns, **0** near-zero-variance columns, **0** exact algebraic duplicates, and all **11** eligible industries produce the **same** zero-variance classification — a positive scalar cannot make a constant column vary, so a disagreement would mean an exposure or an alignment is wrong. Exact-zero *observations* are counted separately from constant *columns*, because a column of zeros and a column that never moves are different facts. Singular values **73.87 / 72.33 / 31.21 / 19.09 / 5.08** (fo600, raw condition **14.54**, standardised **5.29**) and **78.33 / 67.99 / 33.17 / 19.18 / 4.77** (fo1500, raw **16.41**, standardised **5.00**). Pairwise Pearson and Spearman over 15 overlapping months are **descriptive only**: `significance_tests_reported: false`, `assert_no_feature_removal` **raises** on dropping a column for a rank or correlation, and `assert_no_channel_selection` **raises** on "preferred channel", "better rank" and the rest. **Full rank does not promote either variant**: `variant_promoted_to_modeling_readiness: false` | 2026-08-29 |
| AD-R133 | **IND-04 kept, the fuel oils kept apart, and 1,800 cells traced through both chains (Task C10)** | Per variant: **180** rows x 5 columns = 900 cells, **825** numeric and **75** masked; **165** fully numeric rows and **15** fully masked, all IND-04, retained with an explicit cell-level mask. `ind_04_rows_dropped`, `ind_04_masks_converted_to_zero` and `rows_removed_to_make_a_complete_case_table` are all **false**, and `assert_mask_not_zeroed` **raises** on the conversion. The two variants are **never horizontally concatenated**: they carry the SAME sector-093 vector, so a ten-column table would present one coefficient as two independent measurements — `variants_horizontally_concatenated: false`, `ten_column_modeling_matrix_created: false`, `preferred_channel_produced: false`, reported side by side. Lineage was **re-derived independently for all 1,800 cells**, each hashing BOTH provenance branches — C8 transformation digest, its declared input window and status, the C7.5 semantic digest on one side; the sector-093 exposure row, NESDC workbook SHA-256, crosswalk version, gross-output weights, proxy fit, direction decision, frozen C9 Phase-A digest and structural availability month on the other — plus the variant and issue-month selection rule; masked cells included. `assert_upstream_structure_unchanged` **raises** if sector 031 is substituted or the Phase-A digest moves. Availability was checked **against the recorded fields, never by shifting rows**: **0** lag-2 violations and **0** policy violations across all 1,800 cells, `source_available_as_of` null, `point_in_time_supported` false, structural availability 2020-03-31 and never backdated to 2015. `target_joined`, `model_trained`, `model_feature_approved`, `locked_test_accessed`, `target_join_authorized` and `modeling_authorized` all remain **false** | 2026-08-29 |
| AD-R134 | **Fitting the conditioned feature per industry is a reparameterisation, not an architecture (Task C11)** | Candidate A regresses on the common source series per industry; Candidate B regresses on `E[g] * x[t]` per industry. Within one industry `E[g]` is a positive scalar, so the two span the **same column space**: the orthogonal projectors agree to **6.52e-16** (FO 600) and **4.44e-16** (FO 1500) across all **eleven** eligible industries, Candidate B is an exact column rescaling of Candidate A, and per-industry standardisation reproduces A from B exactly. The coefficient map is `gamma[g,h,f] = beta[g,h,f] / E[g]`. No data can distinguish them; only the printed coefficients differ. Recorded as `separate_conditioned_model_is_distinct_architecture: false`, and `assert_separate_conditioned_not_distinct` **raises** if B is claimed as distinct while the projectors coincide. B is rejected on an **algebraic identity, not a comparison** — no development error was computed, and none could have been | 2026-08-30 |
| AD-R135 | **Pooling changes the dimension, not the scale, and that is the entire basis for the selection (Task C11)** | Within one industry `E[g] x[t]` is a rescaling of `x[t]`. In a **pooled** panel it varies across industry **and** time, so the centred interaction is not proportional to the main effect. Verified on a feature-only **165 x 21** panel per variant (5 source, 5 centred interaction, 11 industry indicators, **no** redundant global intercept): source block rank **5**, interaction block rank **5**, combined **10** — exactly the sum, so the blocks are not collinear — full design rank **21 of 21**, **zero** exact duplicate columns, centred-exposure spread **0.0594**. Selected architecture: `pooled_common_effect_plus_centered_sector093_interaction`. Candidate A is **retained as a documented alternative**, not ranked against C: it is a legitimate design that simply does not meet an estimand requiring exposure-associated heterogeneity. All **nine** mandatory gates passed; the config records `expected_selection` and, beside it, `expectation_overrides_a_failed_gate: false`, and `select_architecture` returns **nothing** and names the blocker if any gate fails | 2026-08-30 |
| AD-R136 | **Three constructions destroy the heterogeneity while still fitting, and all three were built rather than argued (Task C11)** | **Constant exposure**: centring makes every value exactly zero, so the interaction block is a zero matrix of **rank 0** — a design that fits and identifies nothing; `assert_exposure_varies` **raises** first. **Redundant global intercept**: an intercept alongside eleven industry indicators is rank-deficient by exactly one, **21 of 22 columns**, a shape that looks correct in every summary; `assert_valid_fixed_effect_coding` **raises** on that coding, and the reference-dummy coding is verified full rank as the permitted alternative. **Per-industry standardisation of the interaction**: within industry `g` the interaction is `e[g] * xtilde`, so standardising it there returns `sign(e[g]) * xtilde` (identity residual **4.44e-16**) and that industry's combined block falls from rank 10 to **rank 5**; the contract therefore forbids it, and the interaction is constructed **after** source standardisation and never rescaled inside an industry. Source location and scale are fitted on the **15 permitted training issue months only, before replication across industries**, so every eligible industry receives identical scaling; exposure centring uses the frozen eleven-industry vector from the C9 Phase-A table and `assert_exposure_centering_source` **raises** on a target, an outcome or a model fold — exposure is time-invariant and structurally available before development, so the complete vector is not leakage | 2026-08-30 |
| AD-R137 | **A preregistered expectation failed, and the wrong measurement was corrected rather than the finding relaxed (Task C11)** | The frozen config expected per-industry standardisation to drop the **stacked** combined rank from 10 to 5. Observed: the stacked rank stayed **10**, for both variants. The preregistered number was the wrong measurement of a real collapse — the centred exposures carry **both signs** (3 positive, 8 negative), so `+xtilde` and `-xtilde` span two directions and the stack keeps rank 10. The correct measurements confirm the collapse: **per-industry** combined rank falls to **5**, and **eleven** distinct exposure-proportional interaction blocks collapse to **two** sign groups, so `theta` could be identified only up to a two-group split. The preregistered value was **not rewritten**: `preregistered_values_changed: false`, `expectations_relaxed_to_obtain_a_pass: false`. The gate `pooled_interaction_survives_the_specified_preprocessing` had wrongly conditioned on that counterfactual — making a gate about the *specified* preprocessing depend on a construction the contract forbids — and was corrected to test the specified preprocessing only. That correction was made **after** observing the failure and is recorded as such in the decision artifact: `gate_implementation_corrected_after_observation: true`. C10's own fingerprint discrepancy is carried forward unchanged alongside it — **11** rounded fingerprints against **1** absolute-tolerance equivalence class for FO 600 — with the **absolute-tolerance rule** governing and `assert_tolerance_rule_governs` **raising** if the rounded count is used as the equivalence rule or the discrepancy is suppressed | 2026-08-30 |
| AD-R138 | **165 rows are 15 histories, IND-04 keeps its benchmark, and selecting an architecture authorizes nothing (Task C11)** | Accounting is reported without being converted into power: **165** panel rows but **15** unique issue months and **15** issue-month clusters; 5 source slopes, 5 interaction slopes and 11 industry fixed effects per horizon, horizons fitted separately. `cross_sectional_rows_help_identify_exposure_heterogeneity: true` **and** `cross_sectional_rows_create_additional_fuel_oil_histories: false`; overlapping monthly transformations and serial dependence remain; `full_rank_is_evidence_of_predictive_usefulness: false` and `parameter_feasibility_equals_statistical_power: false`. No p-value, coefficient uncertainty or outcome-related effective sample size was computed. **IND-04** is excluded from the correction because its direction is `mixed_or_ambiguous`, **not** because its exposure is zero: it keeps the operational benchmark prediction, is never converted to an observed zero or forced to `+1`, must not be silently dropped from evaluation denominators, and is absent from the candidate panel by structural exclusion. **FO 600 and FO 1500** stay co-equal separate variants — `primary_channel_selected: false`, `structural_basis_for_channel_preference_available: false`, `simultaneous_channel_entry_permitted: false`, `parallel_exploratory_variants_permitted_in_future_design: true` — and a design naming both is **refused**. All five transformations are retained with `transformations_removed: []`. Finally, `assert_authorization_not_granted` **raises** if the selection sets any of `target_join_authorized`, `development_modeling_authorized`, `locked_test_evaluation_authorized`, `model_fitted`, `target_joined`, `model_feature_approved`, `predictive_performance_computed`, `locked_test_accessed`, `primary_channel_selected` or `architecture_selected_from_outcomes` — all of which remain **false** | 2026-08-30 |
| AD-R139 | **One of C11's nine gates did not pass as preregistered, and the record now says so (Task C11-R1)** | C11 reported all nine mandatory gates as passing. That is true of the **corrected** gate set and **not** of the **originally preregistered** one. `pooled_interaction_survives_the_specified_preprocessing` was preregistered as *"per-industry standardization would reduce the STACKED combined rank from 10 to 5"*; the observed stacked rank was **10**, so the gate as implemented **did not pass**. C11-R1 records `original_preregistered_stacked_rank_expectation: 5`, `observed_stacked_rank_under_prohibited_preprocessing: 10`, `original_gate_implementation_valid: false`, `original_preregistered_gate_set_passed: false`, `gate_implementation_corrected_after_observation: true`, `corrected_gate_set_passed: true` and — decisively — `architecture_decision_cleanly_preregistered: false`. `assert_original_gate_set_not_reported_as_passing` and `assert_correction_not_presented_as_preregistered` **raise** on any wording that collapses the two sets. This entry does not modify AD-R134…AD-R138; the C11 documents are left **byte-identical** and pinned, and the correction lives in a **sidecar erratum**, because a correction edited into a generated file is erased by the next regeneration | 2026-08-30 |
| AD-R140 | **The correction changed which quantity the gate measured, not a tolerance (Task C11-R1)** | No rank tolerance, duplicate-column tolerance or residual tolerance moved: `thresholds_changed_after_observation: false`, `numerical_tolerance_changed: false`, and `assert_not_described_as_a_tolerance_change` **raises** on "loosened the tolerance", "the threshold was relaxed" and their passive forms. Two different questions were being confused. The **incorrect** one — *does the prohibited preprocessing force the entire stacked matrix to rank five?* — is **false**, because the centred exposures carry both signs (**3** positive, **8** negative) so `+Xtilde` and `-Xtilde` span two directions and the stack keeps rank **10**. The **correct** one — *does per-industry standardization preserve continuous exposure magnitude within an industry?* — is also **false**: `standardize(E_g X)` equals `standardize(X)` when `E_g > 0` and `-standardize(X)` when `E_g < 0`, so only the **sign** survives. Measured, both variants: within-industry combined rank falls **10 → 5**, distinct exposure-proportional interaction blocks collapse **11 → 2**, sign-identity residual **4.44e-16**, while the *selected* preprocessing retains interaction rank **5**. The corrected gate is valid on mathematical grounds and is **still** a post-observation specification correction; reproducibility does not convert it into preregistration | 2026-08-30 |
| AD-R141 | **A development metric artifact was opened during C11, and no later guard undoes that (Task C11-R1)** | **Six** D-series JSON documents were opened during C11; **one**, `docs/d3_operational_baseline_results.json`, had development MAE values displayed. It was **not required**: the benchmark contract name `operational_persistence_latest_published` was already present in the decision log, the methodology and the README, all non-result documentation. Recorded exactly and in both directions: `development_metric_artifact_accessed: true`, `development_target_values_accessed: false`, `locked_test_accessed: false` with `locked_test_manifest_opened: true` and `locked_test_outcomes_accessed: false`, `metric_values_used_in_architecture_decision: false`, `metric_artifact_required_for_decision: false`, **`outcome_free_execution: false`**, `decision_reproducible_without_metric_artifact: true`, `later_guard_retroactively_erases_access: false`, `clean_rerun_restores_original_outcome_blindness: false`. No umbrella `outcome_free: true` appears anywhere — `assert_no_umbrella_outcome_free_claim` walks the whole payload and **raises** on one — because a dependency-clean decision and an outcome-blind execution history are different facts. Remediation is **forward-only**: the C11 runner now blocks D-series paths and C11-R1 admits inputs by an **allowlist**, since a denylist admits every artifact nobody anticipated, which is exactly how the access happened | 2026-08-30 |
| AD-R142 | **The architecture is dependency-clean: reproduced exactly with zero D-series paths opened (Task C11-R1)** | Three instruments, because each alone is weak. **Controlled path guard**: every input admitted by name from a frozen allowlist; **12** permitted entries, **0** D-series paths opened. **Static dependency inspection**: an AST pass over the C11 and C11-R1 chain resolving module-level path constants at **24** read call sites — **0** open a forbidden path, **0** offending imports — and it examines only call-site arguments, so a prohibition list never registers as a read; unresolved arguments (a bare parameter inside a helper) are **counted and reported**, not swallowed. **Metric-substitution invariance**: arbitrary fabricated metric values (`macro_industry_mae: 999.0`, `rmse: -17.5`, `high_stress_outcome_rate: 0.42`, per-channel performance) injected into an isolated fixture, never written to disk and never read from an artifact — the selected architecture **did not move**. Reproduced from C8/C9 alone, both variants: source rank **5**, interaction rank **5**, combined **10**, full **21 of 21**, constant-exposure interaction rank **0**, invalid coding **21 of 22**, Candidate-B projector residual **≤ 6.52e-16**, panel rows **165**, IND-04 ineligible, and the reproduced candidate-design checksums match C11's exactly. Conclusion `architecture_decision_dependency_clean: true`, and `assert_dependency_conclusion_bounded` **raises** if it is ever used to claim the original execution was outcome-blind | 2026-08-30 |
| AD-R143 | **Status: exploratory, supported after a documented specification correction — and D5 is bounded (Task C11-R1)** | `architecture_decision_status: exploratory_supported_after_documented_specification_correction`, with `architecture_mathematically_supported: true`, `architecture_confirmatorily_selected: false`, `architecture_selection_outcome_independence_proven_by_dependency: true`, `original_process_outcome_blind: false`, `eligible_for_confirmatory_claims: false`, `eligible_for_locked_test: false`. An imperfect process does not invalidate a reproduced identity, and a convincing correction does not upgrade an exploratory selection — `assert_status_is_exploratory` **raises** in both directions. The selected architecture's scope is preserved unchanged: interaction after source scaling, **no** per-industry interaction standardisation, industry fixed effects with **no** redundant global intercept, horizons fitted separately, IND-04 benchmark passthrough, FO 600 and FO 1500 as separate variants, no simultaneous entry, no outcome-based promotion, non-causal interpretation. All **nine** closure gates passed, so **D5 is authorized within bounds**: `d5_protocol_preregistration_authorized`, `d5_development_target_join_authorized` and `d5_exploratory_modeling_authorized` **true**; `d5_channel_selection_authorized`, `d5_confirmatory_claims_authorized`, `d5_purge_evaluation_authorized` and `d5_locked_test_evaluation_authorized` **false**. There is no partial grant: `authorize_d5` returns every field false if any closure gate fails, and `assert_d5_authorization_bounded` **raises** on a permission granted despite a failed gate | 2026-08-30 |
| AD-R144 | **The target reader is a lock, and it recorded zero early reads (Task D5)** | D5 runs in two phases. Phase A reproduces the C11-R1 authorization and the C9/C10/C11 invariants, writes the frozen protocol to disk, **reads it back** and hashes what is actually on disk — an in-memory digest proves nothing about the artifact a later reader would see. Frozen protocol `13a0b1846df33f2d81eae8be8415b66bdeaef1bb20642e0a07645774c624483a`. Only then does `GuardedTargetReader` open, and it keeps **two** locks: the protocol lock, shut until that checksum matches, and a **per-prediction freeze lock**, so an observed value is returned only for a key already registered as final. Observed: **720** target reads, **720** predictions frozen, **`target_reads_before_prediction_freeze: 0`**. Every temporal cutoff is asserted independently of the shared contract implementation rather than inherited from it: stress month strictly before `t` (`assert_stress_month_permitted`), calibrator cut at `t-1` (`assert_calibrator_cutoff`), feature reference exactly `t-2` checked against the **recorded** field, label published by `t`. All **15** development origins; `assert_no_purge_or_locked_origin` **raises** on 2025-04…2025-06 and 2025-07…2026-04 | 2026-08-30 |
| AD-R145 | **One estimator, and the 1/n is what keeps it one estimator (Task D5)** | `fixed_pooled_partial_ridge_v1` minimises `(1/n) sum (r - rhat)^2 + lambda(||beta||^2 + ||theta||^2)` with **lambda = 1.0**, no alpha grid, no inner CV, no model averaging, no fallback, no outcome-based feature removal. The normalisation is not cosmetic: with an unnormalised sum the same lambda is a **weaker penalty on every larger panel**, and the panel grows from **231** rows at the first outer issue to **385** at the last, so the specification would drift across the walk-forward without a number being edited — `assert_loss_is_normalised` **raises** on that form. The penalty matrix is **explicit and checked block by block**: the five source and five interaction coefficients penalised, the **eleven industry fixed effects not**, because shrinking an industry intercept toward zero shrinks it toward a stress score of zero, which is a strong claim rather than a neutral prior. No global intercept accompanies the full indicator block (it would make the system singular and let ridge leak into an unpenalised block). Maximum normal-equation residual across all **60** fits: **8.88e-16** | 2026-08-30 |
| AD-R146 | **Counts reconcile exactly: 720 predictions, and 21/35/18/32 training issues (Task D5)** | **720** predictions = 2 variants x 2 horizons x 15 issues x 12 industries, **720** distinct canonical keys, **720** distinct lineage checksums, **660** pooled-model and **60** IND-04 passthrough, **180** per variant-horizon (165 modelled + 15 passthrough). Training issues reconcile against the preregistered table for **both** variants: h=1 **21** issues / **231** eligible rows / **252** all-industry at 2024-01 and **35 / 385 / 420** at 2025-03; h=3 **18 / 198 / 216** and **32 / 352 / 384**. Three clocks must agree for an issue to train: its label published by `t`, its feature reference `u-2` carrying **all five** transformations (the first such month is **2022-01**, which is exactly what removes one h=1 issue at each end), and its operational benchmark history complete at `u`. The two exclusion reasons are **counted separately**, because *not enough price history* and *not enough stress history* are different facts and one total would hide that h=3's start is bound by the **benchmark** history. No unavailable source value is imputed — `assert_no_imputation` **raises** — and pre-2022 stress history is never synthesized | 2026-08-30 |
| AD-R147 | **The result is INCONCLUSIVE for all four variant-horizons, and event safety failed (Task D5)** | Macro-industry MAE on the full twelve-industry panel, model versus primary benchmark: h=1 **17.111 vs 17.094** (FO 1500) and **17.167 vs 17.094** (FO 600); h=3 **18.210 vs 16.634** and **18.207 vs 16.634**. The model is **not better on the point estimate anywhere**. Paired 95% intervals, clustered by issue month with all industries kept together and identical resamples for model and benchmark, all **straddle zero**: [-0.822, 0.847], [-0.096, 3.589], [-0.761, 0.907], [-0.016, 3.525]. Under the frozen rule — supported only if the entire interval is below zero, adverse only if entirely above — MAE evidence is **`inconclusive`** in all four cases, and **event safety FAILED** in all four (model false negatives exceed the benchmark's). Overall status **`inconclusive`** everywhere; `exploratory_incremental_signal_supported` was never reached. The h=1 benchmark macro MAE **17.094469** reproduces D3's registered value exactly, from the formula rather than by copying a metric artifact — verified independently at max gap **0.0** across all 720 benchmark scores | 2026-08-30 |
| AD-R148 | **IND-04 passes through, the channels stay apart, and nothing is approved (Task D5)** | IND-04 receives **no fuel-oil predictor, no fitted fixed effect and a residual correction of exactly 0.0**; its prediction **is** the registered horizon-specific benchmark, verified identical across all 60 rows. That is a **structural exclusion**, not a coefficient estimated at zero, and `assert_passthrough_not_a_fitted_zero` **raises** on the second description. It stays in the **primary twelve-industry denominator**; the eleven-industry eligible-only figure is a preregistered **secondary diagnostic** that `may_replace_the_full_panel_result: false`. FO 600 and FO 1500 are fitted independently — no shared fit, feature table, penalty, average, composite or stacking — and are reported side by side with **no winner**: `channel_selected: false`, `primary_channel_selected: false`, and `assert_no_channel_promotion` **raises** on ranking them by MAE or event metrics. Uncertainty is clustered by **issue month** (h=1 block length 1, h=3 block length 3, seed 20260830, 1000 replications), never by industry row, and every interval carries the note that **fifteen clusters give limited resolution**. Final scope: `evaluation_scope: development_only`, `confirmatory_evaluation: false`, `purge_evaluated: false`, `locked_test_accessed: false`, `locked_test_evaluation_authorized: false`, **`model_feature_approved: false`** | 2026-08-30 |
| AD-R149 | **The EPPO fuel-oil modeling channel is closed, and the closure says exactly what it means (Task C12)** | Channel `eppo_sector093_fuel_oil` is `closed_after_registered_development_evaluation`. The conclusion is `incremental_signal_not_demonstrated_in_registered_development_evaluation` — **not** `fuel_oil_has_no_effect`, `no_predictive_relationship_exists`, `null_hypothesis_proven`, `structural_exposure_is_invalid` or `EPPO_data_is_invalid`, each of which is refused **by name** and in prose form by `assert_no_absence_claim`. Recorded explicitly: development evidence is **inconclusive, not proof of absence**; point estimates provide no positive support; event safety fails in **all four** registered evaluations; **fifteen** issue-month clusters limit uncertainty resolution; the preregistered stop rule nevertheless supports closing further investment; and closure is a **project-governance decision**, not a population-level scientific conclusion (`absence_of_effect_proven: false`). The language guard strips backticked and quoted spans before checking prose, because a guard that could not tell a *named* forbidden term from an *asserted* one would forbid the closure from publishing its own prohibition list. **46** terminal-evidence checks reproduce from the frozen D5 artifacts with `metrics_recomputed_in_c12: false` and `d5_rerun_in_c12: false` | 2026-08-30 |
| AD-R150 | **The evidence chain is recorded in full, because the short version is wrong in both directions (Task C12)** | "C10 proved exposure is useless" would misstate two stages at once. **C10 established a design limitation** — the industry conditioning contributes cross-industry scale and no temporal degrees of freedom, a property of the design matrix established without reading any target — and it neither did nor could show the channel carries nothing. **D5 supplied the development evidence**, and that evidence was inconclusive. The chain is recorded stage by stage: **C7/C7.5** source documents and semantic mapping remain valid, latest-vintage values, policy-based rather than measured publication timing, FO 600 and FO 1500 distinct products and separate variants; **C8** five transformations constructed correctly with missing months propagated **without imputation**; **C9** sector-093 exposure correct but a **broad refinery-product proxy** rather than product-specific fuel-oil consumption, purchasers' price versus ex-refinery still partially mismatched, IND-04 direction-ambiguous; **C11/C11-R1** the pooled common-effect plus centred-exposure interaction is the only selected architecture preserving exposure-associated heterogeneity, mathematically identified, selected **exploratorily after a documented specification correction**, with no alternative authorized from development outcomes; **D5** no variant-horizon point estimate beat its benchmark, intervals inconclusive, event safety failed everywhere | 2026-08-30 |
| AD-R151 | **Closure applies to modeling use only: sources and structure stay valid and preserved (Task C12)** | Eight questions, answered separately. Are the official EPPO source documents still valid evidence? **Yes.** Are the C7/C7.5 semantic decisions retained? **Yes.** Are the C8 transformations reproducible? **Yes.** Is sector-093 exposure reproducible? **Yes.** Is the interaction architecture mathematically identifiable? **Yes.** Was predictive utility demonstrated on development data? **No.** May the channel enter further operational modeling? **No.** May the locked test be opened for this channel? **No.** `assert_source_validity_preserved` **raises** if closure is used to mark a source row, a transformation or an exposure erroneous — a model that failed to beat a benchmark says nothing about whether its inputs were measured correctly. **19 artifacts pinned, 0 deleted, 0 modified**, spanning C7 through D5: ten content checksums (durable identity, timestamps excluded) and nine byte digests (a forward integrity baseline for the generated tables). `preservation_implies_continued_modeling_eligibility: false`, and the existing runners remain able to reproduce their historical outputs under their own frozen contracts | 2026-08-30 |
| AD-R152 | **A new specification is not new evidence, and the bar was set before anyone wanted to clear it (Task C12)** | Sixteen reopening reasons are prohibited by name because each re-describes the evidence D5 already produced: another alpha, an expanded grid, a different estimator, nonlinear terms added after seeing D5, feature selection or transformation removal from D5 coefficients, selecting FO 600 or FO 1500 from observed MAE, combining the channels, PCA or a composite index, tuning the high-stress threshold, evaluating a favourable subgroup, removing IND-04 from denominators, switching direct exposure to total requirement *because D5 failed*, reducing the publication lag, opening purge or locked origins, and rephrasing an inconclusive result as evidence for another specification. Reopening requires evidence **external to D5's performance** in one of six categories — a product-specific consumption crosswalk, a newer compatible I/O table available before the evaluation period, verified point-in-time release evidence, a materially longer compatible target history obtained without purge or locked origins, a proven material implementation error, or a genuinely new external hypothesis registered before any result is inspected — carrying **all nine** required fields including a checksum, an availability date, an explicit account of why D5 could not have used it, an attestation that it was not selected from D5 outcomes, and a superseding decision-log entry. `assert_reopening_evidence` **raises** on a missing or empty field, on `not_selected_from_d5_outcomes: false`, and on **any** record carrying a D5 outcome field such as `model_mae` or `paired_ci_low` | 2026-08-30 |
| AD-R153 | **The guard refuses modeling and permits everything else, with a reason rather than a shrug (Task C12)** | `assert_modeling_entry_permitted` rejects both variants, their aliases (`fo600`, `fuel_oil_1500`, `eppo_ex_refinery_fo600_2s` and the rest, matched after case- and separator-normalisation so a respelling cannot walk past), their composite forms (`fuel_oil_composite`, `fo_pca`, `sector093_fuel_oil_composite`), and direct access to the **C8/C9/C10/C11 feature tables** — for the six blocked purposes `modeling`, `model_training`, `target_assembly`, `feature_selection`, `operational_modeling` and `locked_test_evaluation`. It permits `audit`, `reproduction`, `documentation`, `source_quality`, `provenance_verification` and `historical_d5_reproduction` untouched, and an **unknown** purpose **raises** rather than defaulting to allowed. A refused feature is reported **with its closure reason and the statement that it is present and reproducible** — never as missing, because "not found" would send the next reader hunting a bug that does not exist. **30 guard probes** were run from both sides at closure time: every permitted purpose allowed, every blocked purpose refused, unrelated commodity features unaffected, `generic_upstream_feature_creation_altered: false`, `historical_d5_reproducibility_preserved: true`. Final declaration: the channel is closed, incremental signal was not demonstrated, absence of an effect was **not** proven, official source and structural artifacts remain preserved and valid within their stated limitations, neither variant is selected, no further specification search is authorized, the locked final test remains **unopened**, and reopening requires genuinely new external evidence | 2026-08-30 |
| AD-R154 | **The development programme closed without a supported incremental commodity model (Task E1)** | Final project status, frozen before any result artifact was read and reconciled against every source: `development_program_status: closed_without_supported_incremental_commodity_model`, `registered_operational_reference_available: true` from **D3**, `operational_model_selected: false`, `production_deployment_authorized: false`, `commodity_incremental_signal_supported: false`, `model_feature_approved: false`, `confirmatory_claim_authorized: false`, `locked_test_status: sealed_unopened`, `locked_test_outcomes_read: false`, `fully_real_time_backtest: false`, `latest_vintage_target_evaluation: true`, `future_reopening_requires_new_external_evidence: true`. The phrase **"project failed" is refused by name**: the engineering and evaluation programme completed and produced a reproducible pipeline, a registered operational reference and four honest results — what did not happen is that a candidate earned promotion, and `assert_no_project_failed_language` keeps those two sentences apart. **26 numeric claims** were resolved against their artifacts and reconciled at a 0.001 tolerance; **26 claims** were registered, each with a status from the fixed seven-value vocabulary, a JSON pointer into a loaded artifact, its required limitations, and `locked_test_dependency: false` | 2026-08-30 |
| AD-R155 | **Six collapses that would flatter the record are refused individually (Task E1)** | A single "be careful" catches none of them, so each has its own guard. **Inconclusive is not no effect** — D5's intervals crossed zero on fifteen clusters. **Not supported is not disproven** — a model that did not beat its benchmark has not disproved the hypothesis it encoded. **Unresolved is not absent** — a quantity nobody measured is not zero. **A selected reference is not a proven superior** — D3's paired comparison against no-contraction was **inconclusive** at both horizons and persistence is the *registered* reference through a preregistered tie-break, so `assert_baseline_reference_not_superiority` **raises** on "statistically superior". **A development evaluation is not a production validation** — `assert_d3_not_a_production_model` **raises** on "deployed model", while permitting "production data", which in this domain means manufacturing output and forbidding it would distort the prose rather than protect the claim. **A latest-vintage evaluation is not a real-time backtest** — commodity *features* are archived first-release with verified dates, but the *target* is latest vintage for every source. Each guard strips backticked and quoted spans and skips negated occurrences, so the report can publish its own prohibition list and write "has not disproved" without tripping the rule it is stating | 2026-08-30 |
| AD-R156 | **The full development ledger, negative and superseded results included (Task E1)** | **D1**: h=1 model **15.3073** vs benchmark **14.5287**; h=3 **16.1072** vs **14.6985**; status `not_supported`, **not** operationally release-timing correct, nested protocol **not fully executed**, **6 of 30** origin-horizon selections used the conservative fallback with `fallback_effect_direction: unknown`, `presented_as_confirmatory: false`, **superseded by D3/D4** for operational interpretation. **D3**: h=1 `operational_persistence_latest_published` **17.0945** against no-contraction **19.4453**; h=3 `operational_persistence_trailing_3m_max` **16.6338** against **16.9791**; paired comparison **inconclusive**, `is_a_production_model: false`. **D4**: h=1 **17.5616** vs **17.0945**, interval **[-0.2060, 1.3736]**; h=3 **17.9051** vs **16.6338**, interval **[0.4317, 2.1098]**; MAE evidence `incremental_signal_not_supported` at both horizons with event safety **passed** at both — and `event_safety_overrides_mae_result: false`, because both gates were required and one failed; **180 of 360** primary-panel rows were benchmark passthrough since every eligible direct predictor is a structural constant for six industries. **D5**: four variant-horizons, **0** point estimates beating the benchmark, **4** intervals crossing zero, **4** event-safety failures, `incremental_signal_demonstrated: false` and `absence_of_effect_proven: false`. A superseded result **stays in the ledger** with its marker: deleting a negative result is how a record starts lying | 2026-08-30 |
| AD-R157 | **The locked final test was verified sealed without being opened (Task E1)** | The reservation document is **key-only** — issue months and nothing else — which is exactly what lets the closeout confirm the horizon-1 range **2025-07..2026-04**, the horizon-3 range **2025-07..2026-02**, the **10** reserved months and the **2025-04..2025-06** purge buffer by reading keys alone. `assert_locked_outcomes_untouched` **raises** if the manifest carries any outcome-shaped field, because **hashing an outcome would still be reading it**, and the reproducibility manifest **records no locked path** at all. Every candidate's authorization was checked and none holds it: D1 `approved_for_locked_test: false`, D4 false at both horizons, D5 `locked_test_evaluation_authorized: false`, C12 the same. Required language, present verbatim in the closeout: *"The locked final test was intentionally left unopened because no independently preregistered candidate earned access through development evidence."* The words "passed", "validated" and "unused data proving generalization" are refused by name | 2026-08-30 |
| AD-R158 | **Every reported number carries a pointer, and the test count was measured rather than typed (Task E1)** | The claim registry refuses an entry whose evidence artifact was never loaded — a pointer into a file nobody opened is not evidence — and refuses a `superseded` status without a `superseded_by` marker, a claim with `locked_test_dependency: true`, a duplicate `claim_id`, and any claim that omits a limitation its source artifact requires (D1 without its fallback caveat, D4 without its degenerate-design caveat). Two incompatible evaluation contracts may not be compared without qualification: D1 predates the issue-month contract, so its **15.31** and D4's **17.56** are not comparable numbers and the guard **raises** if they appear together without saying so. The supersession graph is checked **acyclic** (D1→D3, D1→D4, D3→D3-R1, C11→C11-R1). The reproducibility manifest **runs the suite and parses its own summary line** — `assert_measured` **raises** on a supplied literal, because a number typed into a manifest drifts the moment a test is added and a stale count looks like evidence — and it **references** the raw retrieval manifests rather than duplicating them. Two portfolio artifacts are additionally guarded against claiming the model beat its benchmark, was deployable, was real-time, or proved commodities carry no signal, and against printing internal task codes in the reader-facing narrative | 2026-08-30 |
| AD-R159 | **The public-release allowlist fails closed, and every path on disk is deliberately classified (Task E2)** | The release set is built from the **filesystem and git**, never inferred from documentation: a README that says what ships is a claim, and E2 exists to check the claim. **1,909 files / 117.9 MiB on disk; 441 files / 13.9 MiB admitted.** A path matching no rule is **excluded and reported**, and after classification **0** paths fall through that rule — every file is deliberately placed in one of 21 categories. Exclusion always beats inclusion, so a sloppy include cannot admit a dangerous directory; the **14** files that escape a directory-wide exclusion (8 retrieval manifests, 6 data-directory READMEs) are enumerated as **literal paths** and must *also* match an include glob, which makes admitting anything from an excluded directory take two independent decisions. `fnmatch` was rejected for the matching: its `*` crosses directory separators, so `src/**/*.py` would silently miss `src/config.py` while `docs/*.md` would match `docs/architecture/decision_log.md`, and both errors change what ships. Locked-outcome paths are matched **by name and never stat-ed** — a size read is already an inference about contents. Nothing was published: no commit, tag, push, PyPI upload or GitHub release | 2026-08-31 |
| AD-R160 | **Two undeclared-dependency defects found and fixed, and the CI interpreter corrected after measurement (Task E2)** | An AST scan of every import in `src/`, `scripts/` and `tests/` — not a text search, because an import statement written inside a docstring is not an import — found **`pypdf` imported but undeclared** (it worked only because it happened to be installed, exactly the defect a clean-tree install exists to catch) and **`python-dotenv` declared but never imported** (nothing calls `load_dotenv`; the project reads `os.environ` directly). The first was added, the second **removed**: a declaration nothing uses is a claim about the code that is not true. `pyarrow` stays as a **justified** unused declaration, reached through the pandas engine argument where no import will ever name it. Locks were resolved in a **pristine** environment and record what was actually installed there (**19** runtime pins, **9** development), platform-scoped and saying so. Reading each pin's declared `Requires-Python` then showed **`numpy==2.5.2` admits 3.12 and above**, so a CI job on the frozen `ci_python: "3.11"` would have failed on install. The frozen value is **preserved** and a correction block records `changed_after_observation: true` with the effective interpreter and the reason; pinning an older numpy was rejected because it would put into the lock a version nothing in this preparation ever ran. The declared `>=3.11` floor in packaging metadata is left alone and recorded as **unverified** — narrowing a package's supported range is the owner's decision | 2026-08-31 |
| AD-R161 | **Every release claim is measured in an isolated tree, and the data-backed tier is proved rather than trusted (Task E2)** | The working directory is not evidence: it has a virtual environment on the path, 110 MB of downloaded workbooks, generated Parquet, an editable install and an exported environment variable. So the tree is built from the allowlist alone, outside the repository, and the install, import, lint, schema parse, package build and hermetic suite are all measured **there** — `ready_because_working_directory_passes_tests: false`. Result: **1,608 passed, 271 skipped, 0 failed**, counts parsed from pytest's own summary line rather than typed. The tree's checksum is **identical before and after** the run, because a suite that writes into its own source tree is a suite whose second run differs from its first. `tests/conftest.py` skips the **109** registered data-backed tests **only when their inputs are absent**, decided from the filesystem rather than a flag — a tier that an environment variable could switch on would eventually be switched on by accident — so a full working tree runs all **1,879** and skips none. The registry is then **verified by removal**: every entry must fail without it, nothing else may fail, and every failure must name a missing input. Deselecting a green test, or letting an unrelated failure hide inside the registry, is how a suite gets quietly weakened to produce a green CI result | 2026-08-31 |
| AD-R162 | **No credential in the release, no secret value in any report, and four scanner states kept apart (Task E2)** | Two scanners ran and their states are distinct: this project's own pattern set (which covers absolute home paths, usernames in paths and captured response headers that a generic tool does not) and **detect-secrets 1.5.0** as an independent second opinion. An unavailable scanner is recorded as **`not_run`, never `clean`** — a scan nobody performed found nothing in the same uninformative sense that an unopened box contains nothing. **0 credential findings.** High-entropy hits numbering in the hundreds were separated as published content checksums by a *check*, not a dismissal: a hex digest counts only when its own line, an adjacent line or an **enclosing mapping key** names it as a checksum, which is why the fortieth entry of a checksum block classifies as well as the first. **2** findings were reviewed and identified as non-credential and **0** remain unreviewed; review entries are keyed on path, family and fingerprint, **never on a line number**, because a suppression that survives an edit stops meaning anything, and an entry matching nothing is reported rather than dropped. No value is ever returned or written — findings carry a family, a path, a line, a length and a truncated fingerprint, and the rendered report is re-checked against the values actually found. Two planted test fixtures were rewritten to be assembled at run time, because a file containing a well-formed key is indistinguishable from a file that leaked one. `pip-audit` 2.10.1 found **0** vulnerabilities across both locks, recorded with its tool version and advisory service and explicitly **not** a permanent guarantee | 2026-08-31 |
| AD-R163 | **The release manifest is reproducible because it does not cover the documents that quote it (Task E2)** | **438 of 441** released files are hashed; the three E2 review documents ship but are **not rows**, because each one records the manifest checksum and a manifest covering them could never reach a fixed point — its checksum would change on every run whether or not anything else did, and a manifest that changes when nothing changed cannot tell anyone whether anything changed. A checksum-list file omits itself for the same reason. Generation timestamps were removed from every shipped E2 document on the same grounds, and the run date is recorded once, by hand, in the checklist. Verified twice over: recomputed within the run, and **byte-identical across two consecutive full runs**. The checksum itself lives in the manifest and the readiness report, not here: a document that quotes it and is also hashed by it would reintroduce the loop this entry is about. Acquisition and generation timestamps and archive build clocks are excluded by construction. Two package builds produce **identical member sets** and neither archive contains a prohibited file type, a raw-data path or a locked-test member; archive *bytes* differ because both formats store a build clock, which is documented rather than claimed away | 2026-08-31 |
| AD-R164 | **The candidate is blocked on a licence decision that is not mine to make, and five limitations name what was not verified (Task E2)** | Status **`release_candidate_blocked`** on exactly one item: no `LICENSE` file exists and the packaging metadata carries a placeholder, so `owner_license_decision_required: true`. MIT is **recommended** and compatible with every dependency licence read from installed metadata, but choosing a licence is choosing what rights someone gives away permanently, and `assert_no_license_asserted` **raises** on any text that states a licence as this project's. `CITATION.cff` stays unwritten because it needs a real name and the metadata has a placeholder; no name, email, ORCID or affiliation was invented. On data, **public download access is not a redistribution right**: all four publishers' terms are `unresolved` or `likely_permitted_but_not_confirmed_inline`, so **no raw byte ships** and what is published instead is the acquisition code, the URLs and a SHA-256 per retrieved file. Five documented limitations name what was **not** verified: the lock is platform- and interpreter-scoped; the declared 3.11 floor was never exercised; the built wheel imports but cannot resolve project resources, because configuration, documentation and mapping tables are repository content and 15 modules locate them beside the source tree (a packaging change with its own test surface, deferred rather than smuggled into a release task); archive member timestamps differ between builds; and a clean dependency audit is one database on one day. A limitation is not a finding that was talked out of being one | 2026-08-31 |
| AD-R165 | **Three owner decisions applied, and Task E2's record preserved rather than rewritten (Task E2-R1)** | The owner chose **MIT**, a **public GitHub source checkout**, and a **Python 3.12** floor — after E2 had already concluded `release_candidate_blocked` on the missing licence and recorded that its original 3.11 expectation was incompatible with the resolved dependency set. Both E2 findings were correct on the evidence available then, so E2-R1 is a **superseding decision, not an edit**: `supersedes_release_decision: E2`, `e2_original_readiness_preserved: true`, `owner_decision_received_after_e2: true`, `historical_findings_rewritten: false`. **Seven** E2 artifacts that record a finding are pinned by sha256 and re-verified *before this task does anything*; a mismatch stops the run, and `verify_e2_pins` **raises** on a modified or missing record, because a superseded finding edited to agree with a later decision stops being a record of what was known when. Two documents were deliberately **not** pinned — the E2 reproducibility guide and the data-licence review — because neither carries a verdict or a finding: they are operating instructions and a licence position, and instructions that have become wrong are a hazard rather than a record. Each was revised with an explicit note of what E2 said and what changed, and the E2 guide now points at its successor instead of preserving stale commands. The eight upstream scientific artifacts were re-checked too: applying an owner decision changes no scientific conclusion | 2026-08-31 |
| AD-R166 | **The MIT grant is scoped to this project's own work, and a guard refuses to extend it (Task E2-R1)** | `LICENSE` carries the MIT text with the owner's copyright line; packaging metadata declares the SPDX expression `MIT` with `license-files`, and the `Unlicensed` placeholder is gone. `verify_license` checks **five required MIT passages** as normalised phrases, the copyright line, the SPDX declaration, the absence of the placeholder anywhere in metadata, and the absence of a deprecated `License ::` classifier — omitted deliberately, because under PEP 639 an SPDX expression supersedes it and setuptools 77+ rejects both together. The grant covers `src/`, `scripts/`, `tests/`, `configs/`, `schemas/`, `docs/`, `data/mapping/` and the retrieval manifests. It grants **nothing** in World Bank, NESDC, OIE or EPPO material, and `assert_mit_not_applied_to_third_party_data` **raises** on any released document that says otherwise: an over-broad licence claim about someone else's data is not a generosity, it is a false statement about rights this project never held. **155** documents were checked and **0** offences found. The boundary is stated in `LICENSE`, `THIRD_PARTY_NOTICES.md` and the data-licence review even though no third-party byte is in the release, because a reader should not have to derive it. `CITATION.cff` stays **unwritten**: it needs a verified personal author, the metadata uses a collective attribution, and `assert_no_citation_file_without_identity` **raises** if one appears while identity is deferred. No name, email, ORCID or affiliation was invented | 2026-08-31 |
| AD-R167 | **A supported Python version must agree in seven places, and disagreement is a blocker (Task E2-R1)** | `minimum_supported_python: "3.12"`, `tested_python_versions: ["3.12"]`, `python_311_supported: false`, `python_311_compatibility_claim_withdrawn: true`, `python_floor_changed_after_e2_observation: true`, `dependency_lock_python_compatible: true`. Both locks were **regenerated in a pristine CPython 3.12 environment** with the resolver version recorded in each header, and every one of the **28** pins — including `numpy==2.5.2`, the pin that made 3.11 impossible — was checked to admit 3.12. `python_consistency` then reads the version out of **seven** files (packaging metadata, the ruff target, both lock headers, the CI workflow, the README and the reproducibility guide) and compares them; a disagreement is a **blocker**, not a note, because version drift is the defect that makes a reproduction guide untrue without anyone noticing. `assert_no_active_python_311_claim` **raises** on text telling a reader 3.11 is supported while **permitting** a record that E2 expected it and found it incompatible — the two are separated by a negation window, the same way this project's other guards separate a prohibition from the thing it prohibits. **3.13 and later are permitted by metadata and are not claimed as tested**: permitted is not tested | 2026-08-31 |
| AD-R168 | **The release is a source checkout, and the wheel is labelled a diagnostic artifact (Task E2-R1)** | `release_distribution_scope: github_source_checkout`, `supported_install_mode: editable_source_checkout`, and `wheel_runtime_supported`, `sdist_runtime_supported`, `pypi_publication_supported`, `pypi_publication_authorized` and `standalone_installed_distribution_supported` all **false**. The E2 packaging finding is preserved and is the reason: fifteen modules resolve repository resources through paths relative to the source tree, so a wheel installs and imports but cannot reach `configs/`, `docs/` or `data/mapping/`. `installed_distribution_resource_resolution_complete: false`, `packaging_limitation_blocking_for_source_checkout_release: false`, `packaging_limitation_blocking_for_pypi_release: true`, `resource_resolution_refactor_authorized: false` — a packaging change with its own test surface, deferred rather than smuggled into a release task. The wheel and sdist are still **built and inspected**, and are excluded from the release manifest; `assert_no_build_artifact_in_manifest` **raises** on any `.whl`, `.tar.gz` or `dist/`/`build/` path, and `assert_source_checkout_scope` **raises** on any text presenting them, or PyPI, as a supported runtime. The supported install is stated with platform-specific commands and two deliberate flags: `--no-deps`, because a project install that re-resolves the lock is not installing from the lock, and `-e`, because that is the only arrangement in which the repository paths resolve. `assert_limitation_is_documented` **raises** if the decision document ever stops stating the limitation — one that stops being written down stops being a limitation and becomes a surprise | 2026-08-31 |
| AD-R169 | **Verified on the interpreter it claims, in a tree built from the allowlist (Task E2-R1)** | The clean-tree verification runs on **CPython 3.12.14**, discovered rather than assumed: `find_python` **stops the run** if no 3.12 interpreter is present, because verifying on a different interpreter would verify a different claim. In that tree: fresh environment, both locks installed, the project installed **editable with `--no-deps`**, **every public module imported**, CLI smoke checks, ruff, every config and schema parsed, the hermetic suite, and the data-backed registry **verified by removal**. Measured and recorded: collected, passed, skipped, failed, install duration and test duration, all from the commands' own output. The release-tree checksum is **identical before and after** the suite. One real defect surfaced and was fixed properly rather than papered over: `ruff check .` in the tree was linting a third-party console script that pip had installed into the verification environment. The tempting fix — a broad `extend-exclude` — would have violated a standing D3-R1 rule against silencing the linter, and that rule caught it. The environment was renamed to `.venv` instead, a name every tool already leaves alone, so no lint configuration was widened at all | 2026-08-31 |
| AD-R170 | **Ready with five documented limitations, and the one next step needs the owner's confirmation (Task E2-R1)** | Status **`release_candidate_ready_with_documented_nonblocking_limitations`**, superseding E2's blocked decision. Cleared: `owner_license_decision_required: false`, `code_license: MIT`, `license_blocker_cleared: true`, `python_contract_blocker_cleared: true`, `source_checkout_contract_verified: true`, `clean_install_verified: true`, `hermetic_ci_verified: true`, `secret_findings: 0`, `dependency_vulnerability_blockers: 0`, `locked_test_release_excluded: true`. `decide_source_release` **raises** if a required limitation is missing, so the verdict cannot be reached by forgetting to mention one; the five required plus the platform-scoped lock are all recorded with a statement and a reason. It is a **separate decision function** from E2's on purpose — E2-R1 gates on licence consistency, version agreement across seven files, build artifacts in the manifest and the integrity of E2's own record, and folding those into E2's blocker list would quietly change what E2's own tests were asserting about E2. The manifest hashes **449 of 452** released files; the three E2-R1 review documents ship but are not rows, because each quotes the manifest checksum. Nothing was published: `committed`, `tagged`, `pushed`, `github_repository_created`, `released_on_github` and `uploaded_to_pypi` are all false, and a synthetic test inspects every subprocess argument list in the runner to prove no publishing command is ever assembled. The single recommended next task is **E3 — Owner-Approved Public GitHub Repository Release**, which may act only after the owner confirms the account or organisation, the repository name, public visibility, the initial release version, commit and tag authorisation, the Git author identity, and the final public-release manifest checksum | 2026-08-31 |

# Release readiness — Task E2

**Status: `release_candidate_blocked`** — release candidate `0.1.0`, contract `e2_release_contract_v1`.

This document is generated by `scripts/run_e2_release_preparation.py`. Every number below was measured in an isolated release tree built from the public allowlist, not in the working directory. Nothing here was published: no commit, no tag, no push, no PyPI upload, no GitHub release.

## Blockers

| blocker | why it is live |
| --- | --- |
| `missing_owner_license_decision` | no `LICENSE` file exists and `pyproject.toml` carries the placeholder `Unlicensed - student portfolio project`. A licence may be recommended but not chosen on the owner's behalf. |

## What was measured

| gate | result |
| --- | --- |
| files on disk | 1909 files, 117.9 MiB |
| intended public release | 441 files, 13.9 MiB |
| paths admitted by default | 0 — the allowlist fails closed; 0 path(s) matched no rule and were excluded |
| locked-outcome paths in the release | 0 |
| locked-outcome paths in git history | 0 (the repository has 0 commits) |
| clean-tree install | `True` on CPython 3.14.3 |
| hermetic suite in the release tree | `1608 passed, 271 skipped` |
| data-backed registry | 109 entries, verified `True` |
| release tree unchanged by its own tests | `True` |
| wheel members | 116, prohibited 0 |
| sdist members | 157, prohibited 0 |
| two builds, identical member sets | `True` |
| credential findings | 0 |
| privacy findings | 2 |
| dependency advisories | 0 |
| release manifest checksum | `aca7b79229065778c7ae9de22903c42b5c7586b434410751cd162f9299975908` (recomputed identical: `True`) |

The suite count is parsed from pytest's own summary line. A total typed into a report drifts the moment a test is added, and a stale count looks exactly like evidence.

## Documented non-blocking limitations

**`lock_is_platform_and_interpreter_scoped`** — The dependency lock was resolved and verified with CPython 3.14.3, Windows AMD64. It pins versions, not platform wheels, and it has not been installed here on Linux or on the declared 3.11 floor.

> Not blocking: Every pinned version publishes cross-platform wheels and CI installs from this exact file, so the first CI run is what verifies the other platform rather than a claim made here.

**`declared_python_floor_unverified`** — pyproject declares requires-python >=3.11 while the verified lock admits 3.12 and above, set by numpy==2.5.2. The declared floor was not exercised: no 3.11 interpreter exists on this machine.

> Not blocking: The floor governs an unlocked install, which resolves its own older dependencies. It is recorded rather than narrowed, because changing a package's supported range is the owner's decision.

**`wheel_install_imports_but_cannot_resolve_project_resources`** — The built wheel installs and imports, but the project's configs, mapping tables and documentation are repository content rather than package data, and 15 modules locate them beside the source tree. Project resources resolvable from a wheel install: False.

> Not blocking: The supported installation for running this project is a source checkout installed with pip install -e ., which is what the clean-tree verification and CI both use. Making the distribution self-contained is a packaging change with its own test surface and belongs in its own task.

**`archive_member_timestamps_differ_between_builds`** — Two builds produce identical member sets and identical file content, but wheel and sdist formats record a build clock in their member headers, so the archives are not byte-identical.

> Not blocking: Content equality is what a consumer can check; byte-identical archives would require a format guarantee that does not exist.

**`dependency_audit_is_a_snapshot`** — The dependency audit reflects one advisory database at one moment. It is not a permanent property of the pinned set.

> Not blocking: It is reported with its tool and scope so it can be repeated, rather than presented as a standing guarantee.

## What this document does not claim

- It does not say the package is secure. A scan is one tool and one advisory database at one moment.
- It does not say the third-party sources may be redistributed. Their terms are unresolved and their bytes are excluded.
- It does not choose a licence. No `LICENSE` file exists and that decision belongs to the owner.
- It does not reopen any modeling result. The development programme closed at E1 and the locked final test remains sealed and unopened.

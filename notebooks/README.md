# notebooks/

EDA and research only. No production logic lives here — anything that needs to run repeatably or
be imported elsewhere belongs in `src/thai_supply_chain_ews/`, not a notebook.

No notebooks exist yet. When added, keep outputs cleared before committing (a
`.ipynb_checkpoints/` ignore rule already exists in `.gitignore`, but cell outputs containing real
data values should also not be committed — prefer clearing outputs, especially once real data is
loaded).

# data/processed

The joined, wide, monthly industry panel — one row per `(industry_id, reference_period)`. See
`docs/architecture/data_contract_industry_month_panel.md` for the full column contract.

Built from `data/interim/` + `data/mapping/`. Excluded from Git. No data has been placed here yet.

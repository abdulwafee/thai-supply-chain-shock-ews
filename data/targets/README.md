# data/targets

Future Industry Stress Score targets — one row per `(industry_id, prediction_date, horizon)`,
`horizon` in `{1m, 3m}` per `configs/model.yaml`. `risk_level` stays null until a train-only
threshold-fitting step populates it (never computed from the full dataset).

Excluded from Git. No data has been placed here yet.
